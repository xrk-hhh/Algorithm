// #include <stdio.h>

// int main()
// {
//     /*
//         需求：不死「徐福时空」
//         题目背景：
//         徐福是秦朝齐地方士。

//         奉秦皇之命，徐福率三千童男童女踏上了寻找传说中的「蓬莱之药」的征途。但最后再也没有回来。

//         徐福最终去了哪里？有没有找到蓬莱之药？这些问题已经无关紧要了。

//         题目描述：
//         时间的流逝可以抽象成对数字序列进行排序所花费的时间。不同排序策略花费的时间是不同的。这里介绍一种人类探索排序过程中具有里程碑意义的一种排序算法：希尔排序。

//         希尔排序可以被视为一种对插入排序的优化。为了研究希尔排序的运行效率，我们希望你实现一个简单的希尔排序的过程。
//         在这之前，我们会规范插入排序的具体流程以及评价一个插入排序的过程的「代价」。

//         输入格式：
//         第一行有两个整数 n,m，分别表示待排序的数组的长度以及希尔排序进行的轮数。

//         第二行有 n 个整数 a1,a2,⋯,an，描述数组里的元素。

//         第三行有 m 个整数 d1,d2,⋯,dm，描述每一轮选取的 d 的值。数据保证 dm=1。

//         输出格式：
//         第一行输出一个整数 cost，表示整个希尔排序过程中插入排序产生的代价之和。

//         第二行输出 n 个整数，表示数组 a 排序后的结果。
    
//         说明/提示：
//         数据范围及约定
//         对于全部数据，保证 1≤n≤10^5，1≤m≤100，cost≤5×10^7，1≤ai≤10^9，1≤di≤n，dm=1。
    
//     */

//     int n, m;
//     scanf("%d %d", &n, &m);

//     int a[n];
//     int i, j, k;

//     for (i = 0; i < n; i++)
//     {
//         scanf("%d",&a[i]);
//     }

//     int cost = 0;
//     // 循环m轮，每轮处理一个步长d（m--：先使用m的值，再自减）
//     while (m--)
//     {
//         int d;
//         // 读取当前轮的步长d（题目保证最后一轮d=1）
//         scanf("%d",&d);
//         // 按步长d分组：共d个分组，分组起始索引k=0~d-1
//         for (k = 0; k < d; k++)
//         {
//             // 规则1：每组的第1个元素（组内相对下标0）无需插入，但代价固定+1
//             cost++;
//             // 分组内插入排序：从组内第2个元素开始（全局索引k+d，组内相对下标1）
//             for (i = k + d; i < n; i += d)
//             {
//                 // 暂存当前待插入元素（组内待插入元素）
//                 int temp = a[i];
//                 // 向前查找插入位置：j从组内前一个元素开始（全局索引i-d）
//                 for (j = i - d; j >= 0; j -= d)
//                 {
//                     // 规则2：每一次比较操作，代价+1
//                     cost++;
//                     // 若前一个元素比待插入元素大，向后移动
//                     if (a[j] > temp)
//                     {
//                         a[j + d] = a[j];
//                     } 
//                     else
//                     {
//                         // 找到插入位置（前一个元素≤待插入元素），终止查找
//                         break;
//                     } 
//                 }
//                 // 规则3：若j<0（待插入元素是组内最小，插入到组内第1个位置），额外+1代价
//                 if (j < 0)
//                 {
//                     // 移动到了序列的第1个位置
//                     cost++;  
//                 }
//                 // 插入待插入元素到正确位置（j+d是最终插入的全局索引）
//                 a[j + d] = temp;
//             }
//         }
//     }

//     printf("%d\n",cost);

//     for (i = 0; i < n; i++)
//     {
//         if (i > 0)
//         {
//             printf(" "); // 非第一个元素前加空格
//         } 
//         printf("%d",a[i]);
//     }

//     return 0;
// }

