// #include <stdio.h>

// int main()
// {
//     /*
//         需求：ABC
//         题目描述：
//         三个整数分别为 A,B,C。这三个数字不会按照这样的顺序给你，
//         但它们始终满足条件：A<B<C。为了看起来更加简洁明了，我们希望你可以按照给定的顺序重新排列它们。

//         输入格式：
//         第一行包含三个正整数 A,B,C，不一定是按这个顺序。这三个数字都小于或等于 100。
//         第二行包含三个大写字母 A、B 和 C（它们之间没有空格）表示所需的顺序。

//         输出格式：
//         在一行中输出 A，B 和 C，用一个 （空格）隔开。
//     */

//     // 方法一：
//     int A,B,C;
//     // 第一行输入三个正整数
//     scanf("%d %d %d",&A,&B,&C);
//     // 先通过三步条件判断排序，使得 A<B<C
//     if (A > B)
//     {
//         int temp = A;
//         A = B;
//         B = temp;
//     }
//     if (B > C)
//     {
//         int temp = B;
//         B = C;
//         C = temp;
//     }
//     if (A > B)
//     {
//         int temp = A;
//         A = B;
//         B = temp;
//     }
//     char x1, y1, z1;
//     // 第二行输入三个大写字母（之间无空格）
//     // 注：第一个空格是为了跳过缓冲区残留的换行/空格
//     scanf(" %c%c%c",&x1,&y1,&z1);

//     // 再通过嵌套式判断输入的字母对应的顺序
//     if (x1 == 'A')
//     {
//         if (y1 == 'B')
//         {
//             printf("%d %d %d",A,B,C);
//         }
//         else
//         {
//             printf("%d %d %d",A,C,B);
//         }
//     }
//     else if (x1 == 'B')
//     {
//         if (y1 == 'A')
//         {
//             printf("%d %d %d",B,A,C);
//         }
//         else
//         {
//             printf("%d %d %d",B,C,A);
//         }
//     }
//     else
//     {
//         if (y1 == 'A')
//         {
//             printf("%d %d %d",C,A,B);
//         }
//         else
//         {
//             printf("%d %d %d",C,B,A);
//         }
//     }

//     // // 方法二：
//     // int num[3];
//     // // 读取三个整数
//     // scanf("%d %d %d", &num[0], &num[1], &num[2]);
    
//     // // 简单排序（从小到大，确保num[0]<num[1]<num[2]）
//     // for (int i = 0; i < 2; i++) 
//     // {
//     //     for (int j = i + 1; j < 3; j++) 
//     //     {
//     //         if (num[i] > num[j]) 
//     //         {
//     //             int temp = num[i];
//     //             num[i] = num[j];
//     //             num[j] = temp;
//     //         }
//     //     }
//     // }
    
//     // char order[4];  // 存储三个字符（加一个结束符）
//     // // 读取字符时加空格，跳过缓冲区残留的换行/空格
//     // scanf(" %s", order);
    
//     // // 字符映射：'A'→0（最小值），'B'→1（中间值），'C'→2（最大值）
//     // printf("%d %d %d", num[order[0] - 'A'], num[order[1] - 'A'], num[order[2] - 'A']);
    
//     return 0;
// }