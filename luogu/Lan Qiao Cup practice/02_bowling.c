// // // #include <stdio.h>

// // // int main()
// // // {

// // //     /*
// // //         需求：保龄球
// // //         题目描述：
// // //         DL 算缘分算得很烦闷，所以常常到体育馆去打保龄球解闷。
// // //         因为他保龄球已经打了几十年了，所以技术上不成问题，于是他就想玩点新花招。

// // //         DL 的视力真的很不错，竟然能够数清楚在他前方十米左右每个位置的瓶子的数量。
// // //         他突然发现这是一个炫耀自己好视力的借口——他看清远方瓶子的个数后从某个位置发球，这样就能打倒一定数量的瓶子。

// // //         1.◯◯◯

// // //         2.◯◯◯ ◯

// // //         3.◯

// // //         4.◯ ◯

// // //         如上图，每个 “◯” 代表一个瓶子。如果 DL 想要打倒 3 个瓶子就在 1 位置发球，想要打倒 4 个瓶子就在 2 位置发球。

// // //         现在他想要打倒 m 个瓶子。他告诉你每个位置的瓶子数，请你给他一个发球位置。

// // //         输入格式：
// // //         第一行包含一个正整数 n，表示位置数。

// // //         第二行包含 n 个正整数 ai，表示第 i 个位置的瓶子数，保证各个位置的瓶子数不同。

// // //         第三行包含一个正整数 Q，表示 DL 发球的次数。

// // //         第四行至文件末尾，每行包含一个正整数 m，表示 DL 需要打倒 m 个瓶子。

// // //         输出格式：
// // //         共 Q 行。每行包含一个整数，第 i 行的整数表示 DL 第 i 次的发球位置。若无解，则输出 0。
    
// // //         说明/提示：
// // //         【数据范围】
// // //         对于 50% 的数据，1≤n,Q≤1000,1≤ai,m≤10^5。
// // //         对于 100% 的数据，1≤n,Q≤100000,1≤ai,m≤10^9。
// // //     */

// // //     /*
// // //         暴力遍历导致的时间复杂度超标，无法处理题目中的大规模数据
// // //     */
// // //     int n;
// // //     scanf("%d",&n);

// // //     int a[100010];
// // //     for (int i = 1; i <= n; i++)
// // //     {
// // //         scanf("%d",&a[i]);
// // //     }

// // //     int Q;
// // //     scanf("%d",&Q);
    
// // //     for (int i = 1; i <= Q; i++)
// // //     {
// // //         int m;
// // //         scanf("%d",&m);
// // //         int find = 0;
// // //         for (int j = 1; j <= n; j++)
// // //         {
// // //             if (m == a[j])
// // //             {
// // //                 printf("%d\n",j);
// // //                 find = 1;
// // //                 break;
// // //             }
// // //         }
// // //         if (!find)
// // //         {
// // //             printf("0\n");
// // //         }
// // //     }

// // //     return 0;
// // // }

// // /*
// //     链地址法（链表法）哈希表————哈希函数分组 + 链表解决冲突
// //     利用哈希表建立 “瓶子数→位置” 的一一映射，查询时直接查表，时间复杂度O(n + Q)（插入O(n)，查询O(1)）。
// // */
// // #include <stdio.h>
// // #include <stdlib.h>

// // #define MAX_HASH 200003 // 桶的总数（选质数减少冲突）

// // typedef struct {
// //     int val;   // 存储瓶子数（键）
// //     int pos;   // 存储对应的位置（值）
// //     int next;  // 链表指针：指向下一个节点在hash_table中的下标
// // } HashNode;

// // HashNode hash_table[MAX_HASH]; // 存储所有链表节点（最多MAX_HASH个）
// // int head[MAX_HASH];            // 每个桶的头节点下标（-1表示空桶）
// // int idx = 0;                   // 记录hash_table的下一个可用位置

// // /*
// //     hash_table：是一个数组，每个元素是一个链表节点，保存 “瓶子数 - 位置” 对，以及指向下一个节点的 “指针”（这里用数组下标模拟指针）；
// //     head：是一个数组，head[h]表示哈希值为h的桶对应的链表头节点在hash_table中的下标；
// //     idx：相当于 “节点计数器”，每插入一个新节点，就把它存在hash_table[idx]，然后idx++。
// // */

// // // 哈希函数
// // int hash(int x) 
// // {
// //     return (x % MAX_HASH + MAX_HASH) % MAX_HASH; // 处理负数（本题x为正，可简化）
// // }

// // /*
// //     作用：将任意整数x（瓶子数）映射到0~MAX_HASH-1范围内的一个 “桶编号”h；
// //     取模MAX_HASH是为了让桶编号不超出head数组的范围；
// //     +MAX_HASH再取模是为了处理x为负数的情况（本题x是正整数，可简化为x%MAX_HASH）。
// // */

// // // 插入哈希表
// // void insert(int val, int pos) 
// // {
// //     int h = hash(val); // 第一步：计算桶编号h
// //     // 第二步：创建新节点，存在hash_table[idx]
// //     hash_table[idx].val = val;
// //     hash_table[idx].pos = pos;
// //     hash_table[idx].next = head[h]; // 新节点的next指向当前桶的头节点
// //     // 第三步：更新桶的头节点为新节点的下标
// //     head[h] = idx;
// //     // 第四步：idx自增，准备存储下一个新节点
// //     idx++;
// // }

// // /*
// //     插入逻辑拆解（结合例子）：
// //     假设插入第一个元素：val=5，pos=1

// //     1.计算哈希值h=5%200003=5；
// //     2.初始时head[5]=-1（桶 5 是空的）；
// //     3.新节点存在hash_table[0]：
// //     hash_table[0].val=5，hash_table[0].pos=1，hash_table[0].next=-1（没有下一个节点）；
// //     4.更新head[5]=0（桶 5 的头节点是hash_table[0]）；
// //     5.idx变为1。

// //     再插入第二个元素：val=200008，pos=2（哈希值h=200008%200003=5，冲突）
// //     1.哈希值h=5（和val=5冲突）；
// //     2.新节点存在hash_table[1]：
// //     3.hash_table[1].val=200008，hash_table[1].pos=2，hash_table[1].next=head[5]=0（指向之前的头节点）；
// //     更新head[5]=1（桶 5 的头节点变为新节点）；
// //     4.idx变为2。

// //     此时桶 5 的链表结构：head[5]=1 → hash_table[1].next=0 → hash_table[0].next=-1（新节点插在链表头部）。
// // */

// // // 查询哈希表
// // int find(int val) 
// // {
// //     int h = hash(val); // 第一步：计算桶编号h
// //     // 第二步：遍历桶h对应的链表
// //     for (int i = head[h]; i != -1; i = hash_table[i].next) 
// //     {
// //         if (hash_table[i].val == val) 
// //         { // 找到目标值
// //             return hash_table[i].pos;   // 返回对应的位置
// //         }
// //     }
// //     return 0; // 遍历完链表没找到，返回0
// // }

// // /*
// //     查询逻辑拆解（结合上面的例子）：

// //     查询val=200008：
// //     1.哈希值h=5；
// //     2.从head[5]=1开始遍历：
// //     i=1：hash_table[1].val=200008（匹配），返回pos=2；

// //     查询val=5：
// //     1.哈希值h=5；
// //     2.从head[5]=1开始遍历：
// //     i=1：hash_table[1].val=200008（不匹配），继续；
// //     i=hash_table[1].next=0：hash_table[0].val=5（匹配），返回pos=1；

// //     查询val=10（不存在）：
// //     1.哈希值h=10；
// //     2.head[10]=-1（桶 10 是空的），直接返回0。
// // */

// // int main() 
// // {
// //     // 初始化哈希表头
// //     for (int i = 0; i < MAX_HASH; i++) 
// //     {
// //         head[i] = -1;
// //     }

// //     int n;
// //     scanf("%d", &n);
// //     for (int i = 1; i <= n; i++) 
// //     {
// //         int a;
// //         scanf("%d", &a);
// //         insert(a, i); // 存储值和位置
// //     }

// //     int Q;
// //     scanf("%d", &Q);
// //     while (Q--) 
// //     {
// //         int m;
// //         scanf("%d", &m);
// //         printf("%d\n", find(m)); // 直接查询
// //     }

// //     /*
// //         主函数逻辑：串联所有步骤

// //         1.初始化head数组：所有桶的头节点设为-1（表示空桶）；
// //         2.读取位置数n：循环n次，读取每个位置的瓶子数a[i]，调用insert(a[i], i)存入哈希表；
// //         3.读取查询次数Q：循环Q次，读取每次的目标m，调用find(m)查询位置并输出（没找到则输出0）。
    
// //         整个逻辑的核心是 “哈希分组 + 链表串冲突”，通过head数组记录每个桶的链表入口，实现高效的插入和查询。
// //     */
// //     return 0;
// // }

// /*

// */

// #include <stdio.h>
// #include <stdlib.h>

// // 定义结构体：绑定“瓶子数”和“对应位置”
// // 原因：排序后位置会打乱，需保留值与位置的对应关系
// typedef struct 
// {
//     int val;  // 存储每个位置的瓶子数（查询的关键字）
//     int pos;  // 存储瓶子数对应的原始位置（最终要输出的结果）
// } Node;

// // qsort排序的比较函数：按瓶子数val升序排列
// // 原因：二分查找要求数组有序，升序排列后可通过值快速定位
// int cmp(const void *a, const void *b) 
// {
//     // 将void指针转为Node指针，比较val字段
//     return ((Node *)a)->val - ((Node *)b)->val;
// }

// // 二分查找函数：在有序的Node数组中找目标值m对应的位置
// // 参数：arr-有序数组，n-数组长度，m-要找的瓶子数
// // 返回：找到则返回对应pos，否则返回0
// int binary_search(Node *arr, int n, int m) 
// {
//     int left = 0;          // 左边界：初始为数组第一个元素
//     int right = n - 1;     // 右边界：初始为数组最后一个元素
    
//     // 二分查找核心循环：在[left, right]区间内缩小范围
//     while (left <= right) 
//     {
//         // 计算中间位置mid：避免(left+right)溢出，等价于(left+right)/2
//         int mid = left + (right - left) / 2;
        
//         if (arr[mid].val == m) 
//         {
//             // 找到目标值：直接返回对应的原始位置pos
//             return arr[mid].pos;
//         } 
//         else if (arr[mid].val < m) 
//         {
//             // 中间值小于目标值：目标在右半区，左边界右移
//             left = mid + 1;
//         } 
//         else 
//         {
//             // 中间值大于目标值：目标在左半区，右边界左移
//             right = mid - 1;
//         }
//     }
//     // 循环结束未找到目标值：返回0（题目要求无解输出0）
//     return 0;
// }

// int main() 
// {
//     int n;
//     scanf("%d", &n);  // 输入位置数n
    
//     // 动态分配Node数组：存储n个位置的“瓶子数-位置”对
//     Node *arr = (Node *)malloc(n * sizeof(Node));
//     for (int i = 0; i < n; i++) 
//     {
//         scanf("%d", &arr[i].val);    // 输入第i+1个位置的瓶子数
//         arr[i].pos = i + 1;          // 原始位置从1开始，绑定到pos字段
//     }

//     // 对Node数组按val升序排序
//     // 作用：将无序的瓶子数转为有序，为二分查找做准备
//     qsort(arr, n, sizeof(Node), cmp);

//     int Q;
//     scanf("%d", &Q);  // 输入查询次数Q
    
//     // 处理Q次查询
//     while (Q--) 
//     {
//         int m;
//         scanf("%d", &m);  // 输入本次要找的瓶子数m
//         // 二分查找m对应的位置并输出
//         printf("%d\n", binary_search(arr, n, m));
//     }

//     free(arr);  // 释放动态分配的内存，避免内存泄漏

//     /*
//         关键逻辑拆解说明

//         1.结构体Node的作用：
//         原始数组的位置和瓶子数是一一对应的，但排序会打乱位置顺序，因此用结构体绑定两者，保证排序后仍能通过瓶子数找到原始位置。
//         2.排序的必要性：
//         二分查找的前提是数组有序，排序后可将 “遍历查找” 的O(n)时间复杂度优化为O(log n)，处理1e5次查询时效率大幅提升。
//         3.二分查找的核心逻辑：
//         通过不断比较中间值与目标值，将查找区间缩小一半（左半区或右半区），直到找到目标或区间为空，时间复杂度为O(log n)。
//         4.边界处理：
//         位置从 1 开始：题目中位置编号是 1~n，因此arr[i].pos = i+1；
//         无解返回 0：严格匹配题目要求，二分查找未找到时返回 0。

//         时间复杂度分析
//         1.排序阶段：
//         O(n log n)（qsort 的底层是快速排序，时间复杂度为O(n log n)）；
//         2.查询阶段：
//         每次查询是O(log n)，Q 次查询为O(Q log n)；
//         3.整体复杂度：
//         O(n log n + Q log n)，对于n, Q≤1e5的规模，完全能在时间限制内运行。
//     */

//     return 0;
// }