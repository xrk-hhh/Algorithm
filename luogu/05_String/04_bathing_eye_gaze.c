// #include <stdio.h>
// #include <string.h>

// int main()
// {
//     /*
//         需求：浴眼盯真
//         题目背景：
//         验题人提示：本题数据在 windows 下生成，即换行符为 \r\n。

//         题目描述：
//         dXqwq 定义一个字符串四元组 (a,b,c,d) 是“浴眼盯真”的，当且仅当：

//         a,b 的首字母均为 y。
//         c 恰好等于 ding。
//         d 恰好等于 zhen。
//         给定四个用空格分隔的字符串 a,b,c,d，保证其只包含小写英文字母，你需要判断 (a,b,c,d) 是否是“浴眼盯真”的。

//         输入格式：
//         本题有多组测试数据。

//         第一行输入一个整数 T，代表测试数据组数。

//         接下来 T 行，每行输入四个用空格分隔的字符串 a,b,c,d。

//         输出格式：
//         对于每组数据输出一行。

//         如果 (a,b,c,d) 是“浴眼盯真”的，输出 Yes，否则输出 No。

//         说明/提示：
//         【样例解释】

//         前两组测试数据满足全部要求。

//         第三，五组测试数据不满足第一条要求。

//         第四，五组测试数据不满足第二，三条要求。

//         【数据范围】

//         本题采用捆绑测试。

//         Subtask 1（50 pts）：保证输入的所有字符串长度均为 4。
//         Subtask 2（50 pts）：无特殊限制。
//         对于 100% 的数据，1≤T≤100，每个字符串长度不超过 10。
//     */

//     int T;
//     scanf("%d",&T);

//     // // 方法一：先判断并标记，再根据标记输出
//     // // 初始化标记数组
//     // int mark[101] = {0};
//     // for (int j = 0; j < T; j++)
//     // {
//     //     char str[4][11];
//     //     // 读取4个字符串（%s自动跳过空格/换行，包括\r\n）
//     //     scanf("%s %s %s %s",str[0],str[1],str[2],str[3]);

//     //     // 用strcmp直接判断字符串是否完全相等，简洁且不易错
//     //     if (str[0][0] == 'y' && str[1][0] == 'y' && strcmp(str[2],"ding") == 0 && strcmp(str[3],"zhen") == 0)
//     //     {
//     //         mark[j] = 1;
//     //     }
//     //     // 不满足则保持mark[j] = 0（无需额外else）
//     // }
    
//     // // 输出结果
//     // for (int j = 0; j < T; j++)
//     // {
//     //     if (mark[j])
//     //     {
//     //         printf("Yes\n");
//     //     }
//     //     else
//     //     {
//     //         printf("No\n");
//     //     }
//     // }
    
//     // 方法二：边读边判边输出，无中间存储，一次循环完成所有操作
//     // 边读边判边输出，无中间存储，一次循环完成所有操作
//     while (T--) 
//     {
//         char a[11], b[11], c[11], d[11];
//         // 一次性读取4个字符串，scanf自动跳过空格/换行（包括\r\n）
//         scanf("%s%s%s%s", a, b, c, d);

//         // 条件判断顺序：先低成本，后高成本；不满足直接跳else
//         if (a[0] == 'y' && b[0] == 'y'  // 先判首字母（成本最低）
//             && c[4] == '\0' && d[4] == '\0'  // 再判长度为4（确保恰好等于）
//             && c[0] == 'd' && c[1] == 'i' && c[2] == 'n' && c[3] == 'g'  // 手动比对c
//             && d[0] == 'z' && d[1] == 'h' && d[2] == 'e' && d[3] == 'n') 
//         {  // 手动比对d
//             puts("Yes");  // 比printf快，直接输出+换行
//         } 
//         else 
//         {
//             puts("No");
//         }
//     }

//     return 0;
// }