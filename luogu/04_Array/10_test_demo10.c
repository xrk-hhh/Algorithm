// #include <stdio.h>
// #include <string.h>

// int main()
// {
//     /*
//         需求：蛇形方阵
//         题目描述：
//         给出一个不大于 9 的正整数 n，输出 n×n 的蛇形方阵。

//         从左上角填上 1 开始，顺时针方向依次填入数字，如同样例所示。
//         注意每个数字有都会占用 3 个字符，前面使用空格补齐。

//         输入格式：
//         输入一个正整数 n，含义如题所述。

//         输出格式：
//         输出符合题目要求的蛇形矩阵。

//         说明/提示：
//         数据保证，1≤n≤9。

//         输出符合题目要求的蛇形矩阵。

//         输入输出样例：
//         输入                输出
//         4                        1  2  3  4
//                                 12 13 14  5
//                                 11 16 15  6
//                                 10  9  8  7


//     */

//     int n;
//     scanf("%d",&n);

//     // 初始化矩阵为0（未填充）
//     int map[n][n];
//     memset(map, 0, sizeof(map));
    
//     // 方向数组：右、下、左、上（行偏移, 列偏移）
//     int dirs[4][2] = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}};
//     // 当前方向索引（初始为右）
//     int dir = 0;
//     // 当前位置（初始在左上角）
//     int x = 0, y = 0;
//     // 要填充的数字（从1开始）
//     int num = 1;
    
//     // 填充所有位置（共n×n个数字）
//     while (num <= n * n) 
//     {
//         // 填充当前位置
//         map[x][y] = num;
//         // 准备下一个数字
//         num++;
        
//         // 计算下一个位置（按当前方向）
//         int new_x = x + dirs[dir][0];
//         int new_y = y + dirs[dir][1];
        
//         // 检查下一个位置是否合法（在范围内且未填充）
//         if (new_x >= 0 && new_x < n && new_y >= 0 && new_y < n && map[new_x][new_y] == 0) 
//         {
//             x = new_x;
//             y = new_y;  // 合法则移动
//         } 
//         else 
//         {
//             // 不合法则切换方向（顺时针转90度）
//             // dir = 0 → 右
//             // dir = 1 → 下
//             // dir = 2 → 左
//             // dir = 3 → 上
//             dir = (dir + 1) % 4;
//             // 按新方向移动
//             x += dirs[dir][0];
//             y += dirs[dir][1];
//         }
//     }
    
//     // 按格式输出矩阵（每个数字占3个字符）
//     for (int i = 0; i < n; i++) 
//     {
//         for (int j = 0; j < n; j++) 
//         {
//             printf("%3d", map[i][j]);
//         }
//         printf("\n");  // 每行结束换行
//     }
//     return 0;
// }