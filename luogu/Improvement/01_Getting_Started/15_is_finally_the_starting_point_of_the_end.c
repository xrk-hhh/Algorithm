// #include <stdio.h>

// int main()
// {
//     /*
//         需求：终于结束的起点
//         题目背景：
//         终于结束的起点
//         终于写下句点
//         终于我们告别
//         终于我们又回到原点
//         ……

//         一个个 OIer 的竞赛生涯总是从一场 NOIp 开始，大多也在一场 NOIp 中结束，好似一次次轮回在不断上演。
//         如果这次 NOIp 是你的起点，那么祝你的 OI 生涯如同夏花般绚烂。
//         如果这次 NOIp 是你的终点，那么祝你的 OI 回忆宛若繁星般璀璨。
//         也许这是你最后一次在洛谷上打比赛，也许不是。
//         不过，无论如何，祝你在一周后的比赛里，好运。

//         当然，这道题也和轮回有关系。

//         题目描述：
//         广为人知的斐波拉契数列 fib(n) 是这么计算的

        
//                     0,n=0
//         fib(n)=     1, n=1
//                     fib(n−1)+fib(n−2), n>1
//         ​

//         ​
        
//         也就是 0,1,1,2,3,5,8,13⋯，每一项都是前两项之和。

//         小 F 发现，如果把斐波拉契数列的每一项对任意大于 1 的正整数 M 取模的时候，数列都会产生循环。

//         当然，小 F 很快就明白了，因为 (fib(n−1)modM) 和 (fib(n−2)modM) 最多只有 M2 种取值，所以在 M2 次计算后一定出现过循环。

//         甚至更一般地，我们可以证明，无论取什么模数 M，最终模 M 下的斐波拉契数列都会是 0,1,⋯,0,1,⋯。

//         现在，给你一个模数 M，请你求出最小的 n>0，使得 fib(n)modM=0,fib(n+1)modM=1。

//         输入格式：
//         输入一行一个正整数 M。

//         输出格式：
//         输出一行一个正整数 n。

//         说明/提示：
//         数据范围
//         对于 30% 的数据，M≤18；
//         对于 70% 的数据，M≤2018；
//         对于 100% 的数据，2≤M≤706150=0xAC666。
//     */

//     int M;
//     scanf("%d",&M);

//     // 无需大数组，只需保存前两项即可（空间优化）
//     int a = 0, b = 1; // a=fib(n), b=fib(n+1)
//     int n = 0;

//     while (1) 
//     {
//         // 检查当前a=fib(n), b=fib(n+1)是否满足条件
//         if (a == 0 && b == 1 && n > 0) 
//         {
//             printf("%d\n", n);
//             break;
//         }
//         // 计算下一项：fib(n+2) = (fib(n+1)+fib(n))%M
//         int c = (a + b) % M;
//         a = b;
//         b = c;
//         n++;
//     }

//     return 0;
// }