// #include <stdio.h>
// #include <math.h>

// int main()
// {
//     /*
//         需求：Cantor 表
//         题目描述：
//         现代数学的著名证明之一是 Georg Cantor 证明了有理数是可枚举的。他是用下面这一张表来证明这一命题的：

//         1/1 1/2 1/3 1/4 1/5 ...
//         2/1 2/2 2/3 2/4 ...
//         3/1 3/2 3/3 ...
//         4/1 4/2 ...
//         5/1 ...

//         我们以 Z 字形给上表的每一项编号。第一项是 1/1，然后是 1/2，2/1，3/1，2/2，…

//         输入格式：
//         整数 N（1≤N≤10^7）。

//         输出格式：
//         表中的第 N 项。
//     */

//     /*
//         ① 1/1
//         ② 1/2   2/1
//         ③ 3/1   2/2     1/3
//         ④ 1/4   2/3     3/2     4/1
//         ⑤ 5/1   4/2     3/3     2/4     1/5

//         第k行有k个元素
//         偶数行---分子递增，分母递减
//         奇数行---分子递减，分母递增
//         i/j
//         i + j = row + 1

//         找出第几行第几个数（列）
//         1 + 2 + 3 + 4 > 7
//         1 + 2 + 3 < 7
//         7 - 4 = 3
//         即第4行第3列
//     */ 

//     // // 方法一：循环找第几行第几列
//     // int N;
//     // scanf("%d",&N);

//     // // row：第几行
//     // // col：第几列
//     // int row, col;
//     // // 初始化sum为0
//     // int sum = 0;
//     // for (int i = 1; ;i++)
//     // {
//     //     sum += i;
//     //     // sum达到N，结束循环
//     //     if (sum >= N)
//     //     {
//     //         // 行数
//     //         row = i;
//     //         // sum减为前一次（小于N）
//     //         sum -= i;
//     //         // 列数
//     //         col = N - sum;
//     //         break;
//     //     }
//     // }

//     // // 若为偶数行
//     // if (row % 2 == 0)
//     // {
//     //     printf("%d/%d",col,row + 1 - col);
//     // }
//     // // 若为奇数行
//     // else
//     // {
//     //     printf("%d/%d",row + 1 - col,col);
//     // }


//     // 方法二：数学公式
//     // 替代原代码的循环累加，减少冗余逻辑，让代码更简洁、效率更高（尤其对大 N 更友好）。
//     /*
//         第k行有k个元素，前k行的总元素数（求和）为 S(k) = k*(k+1)/2（等差数列求和）。
//         我们需要找到最小的行号row，使得前row行的总元素数 ≥ N（即S(row) ≥ N）。
//         通过解一元二次不等式 row² + row - 2N ≥ 0，
//         用求根公式可得：row ≥ [ -1 + √(1 + 8N) ] / 2只需对这个结果向上取整，
//         就是目标所在的行号row（例如 N=7 时，√(57)≈7.54，计算得 3.27，向上取整为 4，正确）。
//     */
//     int N;
//     scanf("%d", &N);

//     // 1. 用公式计算目标所在的行号row（向上取整）
//     double sqrt_val = sqrt(1 + 8 * N);  // 计算√(1+8N)
//     int row = (int)((-1 + sqrt_val) / 2);  // 初步计算行号
//     // 若初步计算的row满足“前row行总和 < N”，则row+1（确保向上取整）
//     if (row * (row + 1) / 2 < N) 
//     {
//         row++;
//     }

//     // 2. 计算当前行的前一行总元素数、列号（和原逻辑一致）
//     int sum_prev = row * (row - 1) / 2;  // 前row-1行的总元素数
//     int col = N - sum_prev;              // 目标在当前行的“列号”

//     // 3. 根据行的奇偶性确定分子分母（逻辑不变，保持简洁）
//     if (row % 2 == 0) 
//     {
//         // 偶数行：分子=列号，分母=行号+1-列号
//         printf("%d/%d", col, row + 1 - col);
//     } 
//     else 
//     {
//         // 奇数行：分子=行号+1-列号，分母=列号
//         printf("%d/%d", row + 1 - col, col);
//     }
//     return 0;
// }