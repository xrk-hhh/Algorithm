// #include <stdio.h>
// #include <string.h>
// int main()
// {
//     /*
//         需求：肥胖问题
//         题目描述：
//         BMI 指数是国际上常用的衡量人体胖瘦程度的一个标准，其算法是 m /  (h ^ 2)，
//         其中 m 是指体重（千克），h 是指身高（米）。
//         不同体型范围与判定结果如下：
//         小于 18.5：体重过轻，输出 Underweight；
//         大于等于 18.5 且小于 24：正常体重，输出 Normal；
//         大于等于 24：肥胖，不仅要输出 BMI 值（使用 cout 的默认精度），然后换行，还要输出 Overweight；
//         现在给出体重和身高数据，需要根据 BMI 指数判断体型状态并输出对应的判断。

//         对于非 C++ 语言，在输出时，请四舍五入保留六位有效数字输出，如果小数部分存在后缀 0，不要输出后缀 0。

//         请注意，保留六位有效数字不是保留六位小数。例如 123.4567 应该输出为 123.457，5432.10 应该输出为 5432.1。

//         输入格式：
//         共一行。
//         第一行，共 2 个浮点数，m,h，分别表示体重（单位为 kg），身高（单位为 m）。

//         输出格式：
//         输出一行一个字符串，表示根据 BMI 的对应判断。特别地，对于 Overweight 情况的特别处理请参照题目所述。
//     */

//     double m, h;
//     scanf("%lf %lf",&m, &h);

//     double BMI;
//     BMI = m / (h * h);

//     char temp[50];
//     // 用%.6g格式化：保留5位小数（对应6位有效数字），g自动去除无效后缀0
//     // snprintf 是安全的字符串格式化函数，保证写入 temp 的内容不会超出数组长度（避免溢出）。
//     snprintf(temp, sizeof(temp), "%.6g", BMI);

//     // 处理末尾可能存在的.（如"123."需改为"123"）
//     // strlen(temp) 计算格式化后字符串的长度（不含末尾 \0）。
//     int len = strlen(temp);
//     if (len > 0 && temp[len - 1] == '.') {
//         temp[len - 1] = '\0';
//     }
//     // 体重过轻
//     if (BMI < 18.5)
//     {
//         printf("Underweight");
//     }
//     // 正常体重
//     else if (BMI >= 18.5 && BMI <= 24)
//     {
//         printf("Normal");
//     }
//     else
//     {
//         printf("%s\nOverweight",temp);
//     }
//     return 0;
// }