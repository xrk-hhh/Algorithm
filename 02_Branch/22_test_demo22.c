// #include <stdio.h>

// int main()
// {
//     /*
//         需求：ISBN 号码
//         题目描述：
//         每一本正式出版的图书都有一个 ISBN 号码与之对应，ISBN 码包括 9 位数字、1 位识别码和 3 位分隔符，
//         其规定格式如 x-xxx-xxxxx-x，其中符号 - 就是分隔符（键盘上的减号），最后一位是识别码，例如 0-670-82162-4就是一个标准的 ISBN 码。
//         ISBN 码的首位数字表示书籍的出版语言，例如 0 代表英语；第一个分隔符 - 之后的三位数字代表出版社，例如 670 代表维京出版社；
//         第二个分隔符后的五位数字代表该书在该出版社的编号；最后一位为识别码。

//         识别码的计算方法如下：

//         首位数字乘以 1 加上次位数字乘以 2 ……以此类推，用所得的结果 mod11，所得的余数即为识别码，如果余数为 10，则识别码为大写字母 X。
//         例如 ISBN 号码 0-670-82162-4 中的识别码 4 是这样得到的：
//         对 067082162 这 9 个数字，从左至右，分别乘以 1,2,…,9 再求和，即 0×1+6×2+……+2×9=158，然后取 158mod11 的结果 4 作为识别码。

//         你的任务是编写程序判断输入的 ISBN 号码中识别码是否正确，如果正确，则仅输出 Right；如果错误，则输出你认为是正确的 ISBN 号码。

//         输入格式：
//         一个字符序列，表示一本书的 ISBN 号码（保证输入符合 ISBN 号码的格式要求）。

//         输出格式：
//         一行，假如输入的 ISBN 号码的识别码正确，那么输出 Right，否则，按照规定的格式，输出正确的 ISBN 号码（包括分隔符 -）。
//     */
//     char str[13];
//     // 注：数组名本身已表示地址，无需加&
//     scanf("%s",str);

//     // 提取前9位数字（转换为数字值，而非ASCII）
//     int num[9] = {str[0] - '0',str[2] - '0',str[3] - '0',str[4] - '0',str[6] - '0',str[7] - '0',str[8] - '0',str[9] - '0',str[10] - '0'};
//     // 计算求和与模11
//     int sum = 0;
//     for (int i = 0; i < 9; i++)
//     {
//         // 用数字值计算
//         sum += num[i] * (i + 1);
//     }
//     int mod = sum % 11;
//     // 确定正确的识别码
//     char identity;
//     if (mod == 10)
//     {
//         identity = 'X';
//     }
//     else 
//     {
//         // 数字转字符（如4→'4'）
//         // 通常更习惯写 '0' + mod，更直观地体现 “以 '0' 为基准偏移 mod” 的逻辑
//         identity = '0' + mod;
//     }
    
//     if (identity == str[12])
//     {
//         printf("Right");
//     }
//     else
//     {
//         // 修正识别码
//         str[12] = identity;
//         // 直接输出字符串
//         printf("%s",str);
//     }

//     // // 简便做法：
//     // char isbn[13];
//     // scanf("%s", isbn);  // 读取ISBN字符串（格式固定为13字符）
    
//     // int sum = 0;
//     // // 提取前9位有效数字（跳过分隔符'-'），计算加权和
//     // // 有效数字位置：0,2,3,4,6,7,8,9,10（共9位）
//     // int pos[] = {0,2,3,4,6,7,8,9,10};  // 预定义有效数字的索引
//     // for (int i = 0; i < 9; i++) 
//     // {
//     //     sum += (isbn[pos[i]] - '0') * (i + 1);  // 字符转数字后加权累加
//     // }
    
//     // // 计算正确的识别码
//     // char correct = (sum % 11 == 10) ? 'X' : ('0' + sum % 11);
    
//     // // 验证并输出结果
//     // if (correct == isbn[12]) 
//     // {
//     //     printf("Right");
//     // } 
//     // else 
//     // {
//     //     isbn[12] = correct;  // 修正识别码
//     //     printf("%s", isbn);  // 直接输出修正后的字符串
//     // }

//     return 0;
// }