// #include <stdio.h>
// #include <stdlib.h>

// int main() 
// {
//     /*
//     需求：计算合作默契度
//     ACM协会非常活跃，不仅仅有打比赛的竞赛组，也有开发组和其他组别。现在会长大人不想我们天天敲代码，举行了我们组与组之间的才艺比拼。有两个组要合作进行才艺表演。每个组有n个组员，每人都有一个才艺值。你需要帮助会长大人计算两个组的合作默契度。默契度的计算规则如下:
//     ，两个组对应位置的人(即第一组的第1个人对第二组的第1个人，第2个对第2个，以此类推)会进行才艺配合
//     每对的配合效果等于他们才艺值的乘积
//     两个组的合作默契度由最终配合效果的合计来衡量
//     最终结果需要对一个质数 p取模
//     数据很大，注意整数溢出

//     输入格式：
//     第一行包含两个整数n和p(1≤n≤10^6,2≤p<10^9+7，且p是质数）
//     第二行包含 n个整数 a1, α2,…, an(0 < a¡≤ 10^9)，表示第一个组员的才艺值
//     第三行包含 n个整数 b,b2,…,b„(0 ≤ b≤ 10^9)，表示第二个组员的才艺值

//     输出格式：
//     输出一个整数，表示两个组的合作默契度(即所有配合效果总和模p的结果)
//     */
//     // 第一行输入 n p
//     long long n, p;
//     scanf("%lld %lld", &n, &p);

//     // 避免内存不足溢出
//     long long* arr = (long long*)malloc(n * sizeof(long long));
//     if (arr == NULL) 
//     {
//         // 检查内存分配是否成功
//         // 失败则退出程序
//         return 1;
//     }

//     // 将所需n个a值扔进数组输入
//     for (int i = 0; i < n; i++) 
//     {
//         // 注：这里的%lld后面无需加空格，因为%lld会自动跳过所有空白字符
//         scanf("%lld", &arr[i]);
//     }


//     long long* arr2 = (long long*)malloc(n * sizeof(long long));
//     if (arr2 == NULL) 
//     {
//         // 释放已分配的内存
//         free(arr);
//         return 1;
//     }

//     for (int i = 0; i < n; i++) 
//     {
//         scanf("%lld", &arr2[i]);
//     }

//     long long sum = 0;
//     /*
//         此处用到模的性质：分步取模
//         乘法：(a * b) % p = [(a % p) * (b % p)] %p
//         加法：(a + b) % p = [(a % p) + (b % p)] %p 
//     */
//     for (int i = 0; i < n; i++) 
//     {
//         // 每次计算乘积后立即取模，再累加并取模，防止溢出
//         long long product = (arr[i] % p) * (arr2[i] % p) % p;
//         sum = (sum + product) % p;
//     }

//     // 输出打印
//     printf("%lld\n", sum);

//     // 释放内存
//     free(arr);
//     free(arr2);
//     arr = NULL;
//     arr2 = NULL;

//     return 0;
// }
