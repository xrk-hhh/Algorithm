// #include <stdio.h>
// #include <math.h>

// // 声明质数判断函数
// int is_prime(int n);

// int main()
// {
//     /*
//         需求：质数口袋
//         题目描述：
//         小 A 有一个质数口袋，里面可以装各个质数。
//         他从 2 开始，依次判断各个自然数是不是质数，如果是质数就会把这个数字装入口袋。
//         口袋的负载量就是口袋里的所有数字之和。
//         但是口袋的承重量有限，装的质数的和不能超过 L。给出 L，请问口袋里能装下几个质数？
//         将这些质数从小往大输出，然后输出最多能装下的质数的个数，数字之间用换行隔开。

//         输入格式：
//         一行一个正整数 L。

//         输出格式：
//         将这些质数从小往大输出，然后输出最多能装下的质数个数。

//         说明/提示：
//         数据保证，1≤L≤10^5
//     */
//     int L;
//     // 输入口袋最大承重L
//     scanf("%d",&L);

//     // 记录已装入质数总和
//     int sum = 0;
//     // 记录装入质数个数
//     int count = 0;
//     // 从2开始检查每个自然数
//     int i = 2;
//     // 无限循环，通过内部条件退出
//     while (1)
//     {
//         // 若当前数是质数
//         if (is_prime(i))
//         {
//             // 先检查加入后质数和是否超过L
//             if (sum + i > L)
//             {
//                 // 超过则退出
//                 break;
//             }
//             // 未超过则加入口袋
//             // 输出该质数
//             printf("%d\n",i);
//             // 计数+1
//             count++;
//             // 总和累加
//             sum += i;
//         }
//         // 检查下一个质数
//         i++;
//     }
//     printf("%d",count);
//     return 0;
// }
// // 质数判断函数
// int is_prime(int n)
//  {
//     // <= 1不是质数
//     if (n <= 1)
//     {
//         return 0;
//     }
//     // 2是质数
//     if (n == 2)
//     {
//         return 1;
//     }
//     // 偶数（>2）不是质数
//     if (n % 2 == 0)
//     {
//         return 0;
//     }
//     // 检查到平方根即可
//     int sqrt_n = (int)sqrt(n);
//     // 只查奇数
//     for (int i = 3; i <= sqrt_n; i += 2)
//     {
//         // 有因数，不是质数
//         if (n % i == 0)
//         {
//             return 0;
//         }
//     }
//     // 是质数
//     return 1;
//  }