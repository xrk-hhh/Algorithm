// #include <stdio.h>

// int main()
// {
//     /*
//         需求：津津的储蓄计划
//         题目描述：
//         津津的零花钱一直都是自己管理。每个月的月初妈妈给津津 300 元钱，津津会预算这个月的花销，并且总能做到实际花销和预算的相同。

//         为了让津津学习如何储蓄，妈妈提出，津津可以随时把整百的钱存在她那里，到了年末她会加上 20% 还给津津。
//         因此津津制定了一个储蓄计划：每个月的月初，在得到妈妈给的零花钱后，
//         如果她预计到这个月的月末手中还会有多于 100 元或恰好 100 元，她就会把整百的钱存在妈妈那里，剩余的钱留在自己手中。

//         例如 11月初津津手中还有 83 元，妈妈给了津津 300 元。
//         津津预计11月的花销是 180 元，那么她就会在妈妈那里存 200 元，自己留下 183 元。到了 11 月月末，津津手中会剩下 3 元钱。

//         津津发现这个储蓄计划的主要风险是，存在妈妈那里的钱在年末之前不能取出。
//         有可能在某个月的月初，津津手中的钱加上这个月妈妈给的钱，不够这个月的原定预算。
//         如果出现这种情况，津津将不得不在这个月省吃俭用，压缩预算。

//         现在请你根据 2004 年 1 月到 12 月每个月津津的预算，判断会不会出现这种情况。
//         如果不会，计算到 2004 年年末，妈妈将津津平常存的钱加上 20% 还给津津之后，津津手中会有多少钱。

//         输入格式：
//         12 行数据，每行包含一个小于 350 的非负整数，分别表示 1 月到 12 月津津的预算。

//         输出格式：
//         一个整数。如果储蓄计划实施过程中出现某个月钱不够用的情况，输出 −X，X 表示出现这种情况的第一个月；
//         否则输出到 2004 年年末津津手中会有多少钱。

//         注意，洛谷不需要进行文件输入输出，而是标准输入输出。
//     */

//     // // 方法一：预算数组+两个循环（模拟题目要求，逻辑清晰，但冗长）
//     // int budget[12];
//     // for (int i = 0; i < 12; i++)
//     // {
//     //     // 读取1-12月预算
//     //     scanf("%d",&budget[i]);
//     // }

//     // // 每月剩余的零头（自己手中的钱）
//     // int left = 0;
//     // // 存在妈妈那里的总钱数（整百部分累计）
//     // int save = 0;

//     // for (int i = 0; i < 12; i++)
//     // {
//     //     // 1. 当月月初的总资金 = 上月剩余零头 + 妈妈给的300元
//     //     int month_start = 300 + left;
//     //      // 2. 检查月初资金是否够当月预算
//     //     if (month_start < budget[i])
//     //     {
//     //         // 输出第一个不够的月份（1-12）
//     //         printf("-%d",i+1);
//     //         return 0;
//     //     }

//     //     // 3. 本月花费后剩余的钱
//     //     int after_month = month_start - budget[i];
//     //     // 4. 存整百部分到妈妈那里，自己留零头
//     //     // 整百部分存入
//     //     save += (after_month / 100) * 100;
//     //     // 零头留在自己手中
//     //     left = after_month % 100;
//     // }

//     // // 5. 年末总资金 = 12月剩余零头 + 存款总额×1.2（题目保证为整数）
//     // int total = left + (int)(save * 1.2);
//     // printf("%d",total);

//     // 方法二：变量跟踪+一个循环（按每月流程顺序处理，流程紧凑）
//     // left：当月剩余零头
//     int left = 0; 
//     // save：累计存款
//     int save = 0;
//     // budget：预算
//     int budget;  

//     for (int i = 0; i < 12; i++) 
//     {   // 读当月预算
//         scanf("%d", &budget);  
//         // 当月初总资金（上月剩+300）
//         int total = left + 300;  

//         if (total < budget) 
//         {  // 资金不足，输出第一个月份
//             printf("-%d", i + 1);
//             return 0;
//         }

//         // 花费后剩余
//         int remain = total - budget; 
//         // 存整百部分
//         save += remain / 100 * 100;  
//         // 留零头
//         left = remain % 100;          
//     }

//     // 年末总资金=剩余零头+存款*1.2（整数）
//     printf("%d", left + (int)(save * 1.2));
//     return 0;
// }