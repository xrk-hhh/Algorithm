// #include <stdio.h>

// int main()
// {
//     /*
//         需求：最长连号
//         题目描述：
//         输入长度为 n 的一个正整数序列，要求输出序列中最长连号的长度。

//         连号指在序列中，从小到大的连续自然数。

//         输入格式：
//         第一行，一个整数 n。

//         第二行，n 个整数 ai ，之间用空格隔开。

//         输出格式：
//         一个数，最长连号的个数。

//         说明/提示：
//         数据规模与约定
//         对于 100% 的数据，保证 1≤n≤10^4，1≤ai≤10^9
//     */

//     int n;
//     scanf("%d",&n);

//     int arr[n];
//     for (int i = 0; i < n; i++)
//     {
//         scanf("%d",&arr[i]);
//     }
//     // 最长连号长度（至少为1，因为至少有一个数）
//     int max_len = 1;
//     // 当前连续段的长度
//     int current_len = 1;

//     // 从第二个元素开始遍历（与前一个元素比较）
//     for (int i = 1; i < n; i++) 
//     {
//         if (arr[i] - arr[i - 1] == 1) 
//         {
//             // 若当前元素是前一个元素的“下一个自然数”，则当前连续段长度+1
//             current_len++;
//             // 若当前连续段长度超过历史最长，更新最长长度
//             if (current_len > max_len) 
//             {
//                 max_len = current_len;
//             }
//         } 
//         else 
//         {
//             // 若不连续，当前连续段长度重置为1（重新开始计数）
//             current_len = 1;
//         }
//     }
        
//     printf("%d",max_len);
//     return 0;
// }