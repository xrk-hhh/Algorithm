// #include <stdio.h>

// int main()
// {
//     /*
//         需求：金币
//         题目背景：
//         NOIP2015 普及组 T1

//         题目描述：
//         国王将金币作为工资，发放给忠诚的骑士。
//         第一天，骑士收到一枚金币；
//         之后两天（第二天和第三天），每天收到两枚金币；
//         之后三天（第四、五、六天），每天收到三枚金币；
//         之后四天（第七、八、九、十天），每天收到四枚金币……；
//         这种工资发放模式会一直这样延续下去：当连续 n 天每天收到 n 枚金币后，骑士会在之后的连续 n+1 天里，每天收到 n+1 枚金币。

//         请计算在前 k 天里，骑士一共获得了多少金币。

//         输入格式：
//         一个正整数 k，表示发放金币的天数。

//         输出格式：
//         一个正整数，即骑士收到的金币数。

//         说明/提示：
//         对于 100% 的数据，1≤k≤10^4
//     */
//     // // 方法一：
//     // // 1. 输入处理：读取需要计算的总天数k
//     // int k;  // 存储输入的天数k
//     // scanf("%d", &k);  // 从键盘读取k的值

//     // // 2. 变量初始化：定义核心变量并赋初始值
//     // int total_coins = 0;  // 用于累计总金币数，初始为0
//     // int total_days = 0;   // 用于累计已计算的天数，初始为0
//     // int days_in_stage = 1;  // 当前"阶段"的天数长度（第1阶段1天，第2阶段2天...）
//     // int coins_per_day = 1;  // 当前阶段每天发放的金币数（与阶段数相同，第1阶段1枚，第2阶段2枚...）

//     // // 3. 核心循环：按阶段计算金币，直到累计天数达到k
//     // // 循环条件：只要已计算的总天数还没到k，就继续处理下一个阶段
//     // while (total_days < k) 
//     // {
//     //     // 3.1 计算当前阶段实际需要计算的天数
//     //     // 当前阶段理论上有days_in_stage天，但可能最后一个阶段不足days_in_stage天（比如k刚好在阶段中间）
//     //     // 因此实际天数 = 剩余需要计算的天数（k - total_days）与当前阶段总天数（days_in_stage）的较小值
//     //     int current_days = days_in_stage;  // 先假设当前阶段能完整计算
//     //     if (total_days + current_days > k) 
//     //     {  // 如果当前阶段完整计算会超过k天
//     //         current_days = k - total_days;  // 则只计算剩余的天数（k - 已累计的天数）
//     //     }

//     //     // 3.2 累加当前阶段的金币数
//     //     // 当前阶段每天发coins_per_day枚，共current_days天，因此总金币增加：coins_per_day * current_days
//     //     total_coins += coins_per_day * current_days;

//     //     // 3.3 更新已累计的天数
//     //     // 把当前阶段实际计算的天数加到总天数中
//     //     total_days += current_days;

//     //     // 3.4 准备进入下一个阶段
//     //     // 下一个阶段的天数比当前多1（阶段n+1有n+1天），每天的金币数也多1
//     //     days_in_stage++;
//     //     coins_per_day++;
//     // }


//     // // 4. 输出结果：当累计天数达到k时，总金币数就是答案
//     // printf("%d\n", total_coins);

//     // 方法二：
//     // k：总天数
//     int k;
//     scanf("%d",&k);
//     // sum：获得的金币总数
//     int sum = 0;
//     // coins_per_day：每天能获得的金币数
//     int coins_per_day = 1;
//     // count：往后几天，获得的金币数都是coins_per_day
//     int count = 0;

//     for (int i = 1; i <= k; i++)
//     {
//         sum += coins_per_day;
//         count++;
//         if (coins_per_day == count)
//         {
//             coins_per_day++;
//             count = 0;
//         }
//     }
//     printf("%d",sum);


//     // // 方法三：
//     // // k：总天数
//     // int k;
//     // scanf("%d",&k);
//     // // coins：金币数
//     // int coins = 0;
//     // // days：累加天数
//     // int days = 0;

//     // // 外层循环：天数自增
//     // for (int i = 1; days < k; i++)
//     // {
//     //     // 内层循环：保持三角形结构
//     //     for (int j = 1; j <= i && days < k; j++)
//     //     {
//     //         // i：表示行数，同时也是当天可得的金币数
//     //         coins += i;
//     //         // days：累加完后等于k，刚好跳出循环
//     //         days++;
//     //     }
//     // }
//     // printf("%d",coins);
//     return 0;
// }