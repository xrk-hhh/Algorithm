// #include <stdio.h>
// #include <stdlib.h>

// int main()
// {
//     /*
//         需求：A-莲子的软件工程学
//         题目背景：
//         在宇宙射线的轰击下，莲子电脑里的一些她自己预定义的函数被损坏了。

//         对于一名理科生来说，各种软件在学习和研究中是非常重要的。
//         为了尽快恢复她电脑上的软件的正常使用，她需要尽快地重新编写这么一些函数。

//         题目描述：
//         具体而言，给定两个整数 a,b，保证 b≠0。
//         莲子要实现这样一个函数 fun(a,b) 来将 b 的符号转移到 a 上。

//         具体而言，fun(a,b)=sgn(b)×∣a∣。
//         其中，sgn(b)={1, b > 0
//                     {-1, b < 0    
//         ​
//         换而言之：

//         如果 b 是正数，那么 fun(a,b)= +∣a∣=∣a∣；
//         如果 b 是负数，那么 fun(a,b)= −∣a∣。

//         输入格式：
//         共一行两个整数 a,b。
        
//         输出格式：
//         共一行一个整数 fun(a,b) 的值。

//         说明/提示：
//         对于全部数据，保证 a,b 在 32 位有符号整型范围内，并且 b≠0。
//     */

//     // 本题坑点：int 32位有符号整型的范围是
//     // -2147483648 ~ 2147483647，其中 最小值 -2147483648 没有对应的正数绝对值
//     // （其绝对值 2147483648 超出了 32 位 int 的最大值）。

//     int a, b;
//     scanf("%d %d",&a,&b);

//     // 1. 确定 b 的符号（逻辑不变）
//     int sgn = (b > 0) ? 1 : -1;

//     // 2. 核心修改：用 long long 处理 a，避免绝对值溢出
//     long long a_ll = (long long)a; // 将 a 转为 64 位整型
//     long long abs_a = llabs(a_ll); // 计算 64 位绝对值（正确处理 INT_MIN）
//     long long fun = sgn * abs_a;   // 结果用 long long 存储，无溢出

//     // 3. 输出 long long 类型（用 %lld 格式符）
//     printf("%lld", fun);

//     return 0;
// }