// #include <stdio.h>

// int main()
// {
//     /*
//         需求：课程报名
//         题目描述：
//         传智播客推出了一款课程，并进行了一次促销活动。
//         具体来说就是，课程的初始定价为 v 元；每报名 m 个学员，课程的定价就要提升 a 元。
//         由于课程能够容纳的学生有限，因此报名到 n 人的时候就停止报名。

//         现在老师想知道，当课程停止报名时，一共可以获得多少学费呢？

//         输入格式：
//         一行四个使用空格隔开的整数，分别为 n,v,m,a。

//         输出格式：
//         一行一个整数，表示答案。

//         说明/提示：
//         数据规模与约定
//         对于 50% 的数据，满足 1≤n,m,v,a≤10；
//         对于额外 20% 的数据，满足 a=0；
//         对于 100% 的数据，满足 0≤n,m,v,a≤1000。

//         然而，由于本次比赛是 ACM 赛制，因此您必须通过 100% 的数据才能够获得本题的得分，后题同。
//     */

//     // // 方法一：直接按照公式模拟， n 比较小时
//     // int n, v, m, a;
//     // scanf("%d %d %d %d",&n,&v,&m,&a);

//     // int total = 0;
//     // // i 表示第几个学员（1~n）
//     // for (int i = 1; i <= n; i++)
//     // {
//     //     // 计算第k个学员的单价：v + 涨价次数 * a
//     //     // 涨价次数 = (i - 1) / m（每m个学员涨一次，前m个学员涨价次数为0）
//     //     int price = v + ((i - 1) / m) * a;
//     //     total += price;

//     // }

//     // printf("%d",total);

//     // 方法二：分批次进行，适合 n 更大时
//     int n, v, m, a;
//     scanf("%d %d %d %d", &n, &v, &m, &a);
//     long long total = 0;
//     int batches = n / m; // 完整的批次数量（每批m人）
//     int remain = n % m;  // 剩余不足一批的人数

//     // 计算完整批次的学费：第 0 批单价v，第 1 批 v + a，...，第 batches - 1 批 v + (batches - 1) * a
//     for (int i = 0; i < batches; i++) 
//     {
//         total += (long long)(v + i * a) * m;
//     }

//     // 计算剩余人数的学费：单价为 v + batches * a
//     if (remain > 0) 
//     {
//         total += (long long)(v + batches * a) * remain;
//     }

//     printf("%lld", total);

//     return 0;
// }