// #include <stdio.h>

// int main()
// {
//     /*
//         需求：最厉害的学生
//         题目描述：
//         现有 N 名同学参加了期末考试，并且获得了每名同学的信息：
//         姓名（不超过 8 个字符的仅有英文小写字母的字符串）、语文、数学、英语成绩（均为不超过 150 的自然数）。
//         总分最高的学生就是最厉害的，请输出最厉害的学生各项信息（姓名、各科成绩）。如果有多个总分相同的学生，输出靠前的那位。

//         输入格式：
//         第一行输入一个正整数 N，表示学生个数。

//         第二行开始，往下 N 行，对于每一行首先先输入一个字符串表示学生姓名，再输入三个自然数表示语文、数学、英语的成绩。均用空格相隔。

//         输出格式：
//         输出最厉害的学生。

//         说明/提示：
//         数据保证，1≤N≤1000，姓名为长度不超过 8 的字符串，语文、数学、英语成绩均为不超过 150 的自然数。
//     */

//     int N;
//     scanf("%d",&N);

//     // 姓名数组列数为9（8个字符 + 1个'\0'终止符）
//     char name[1001][9] = {0};
//     int Chinese[1001] = {0};
//     int math[1001] = {0};
//     int English[1001] = {0};
//     int score[1001] = {0};
//     // 存储最高分学生的索引（比单独存max更直观）
//     int max_idx = 0;

//     for (int i = 0; i < N; i++)
//     {
//         // 去掉&name[i]的&，直接传递name[i]（首地址）
//         scanf("%s %d %d %d",name[i],&Chinese[i],&math[i],&English[i]);
//         score[i] = Chinese[i] + math[i] + English[i];

//         // 用索引记录最高分学生，逻辑更清晰
//         if (score[i] > score[max_idx])
//         {
//             max_idx = i;
//         }
//     }

//     // 直接输出最高分学生的信息（通过索引访问，无需二次遍历）
//     printf("%s %d %d %d",name[max_idx],Chinese[max_idx],math[max_idx],English[max_idx]);

//     return 0;
// }