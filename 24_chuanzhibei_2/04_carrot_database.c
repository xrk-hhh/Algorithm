// #include <stdio.h>

// int main()
// {
//     /*
//         需求：萝卜数据库
//         题目描述：
//         花栗鼠很喜欢偷吃生产队的大萝卜，因此花栗鼠科技大学正在研究一种新型的数据库，叫做萝卜数据库。

//         具体来说，它支持 k(1≤k≤100) 个字段，每个字段名都是整数，里面存储的数值也都是整数。

//         现在你支持如下操作：

//         向数据库中插入一个记录，它可能只会包含 k 个字段的某一部分。具体的操作格式详见“输入格式”。

//         在数据库中查询有多少条符合条件的记录。

//         现在你总共有 n 次操作（1≤n≤1000）,请你对每个回答操作，输出结果。

//         输入格式：
//         第一行两个整数 n,k ，意义如题所述。

//         接下来的若干行，每行代表一次操作，具体如下：

//         1 p x1 y1,...,xp yp：表示一个插入操作，其中共有 p 个字段，第 i 字段的名字是 xi ，值为 yi .
//         此处我们保证 1≤xi≤k,1≤yi≤1000，并且 xi,yi均为整数。

//         2 x ymin ymax：表示一次查询操作，表示查询所有满足 字段 x 的值在 [ymin,ymax] 之间的记录有多少个。

//         输出格式：
//         对于每个查询操作，输出一行一个整数，表示符合条件的记录个数。
    
//     */

//     int n, k;
//     scanf("%d %d",&n,&k);

    
//     // 数据结构设计：records[记录索引][字段名] = 字段值（0表示该字段未插入）
//     // n≤1000，k≤100，二维数组足够存储（1000条记录 × 101个字段名（1~k））
//     int records[1000][101] = {0};  // 字段名从1~k，所以第二维开101（索引0 unused）
//     int record_count = 0;  // 当前已插入的记录数

//     for (int op = 0; op < n; op++) 
//     {
//         int choose;
//         scanf("%d", &choose);

//         if (choose == 1) 
//         {
//             // 插入操作：新增1条记录
//             int p;
//             scanf("%d", &p);
//             // 遍历当前记录的p个字段
//             for (int i = 0; i < p; i++) 
//             {
//                 int x, y;
//                 scanf("%d %d", &x, &y);
//                 // 存储到当前最新记录（record_count）的x字段
//                 records[record_count][x] = y;
//             }
//             record_count++;  // 记录数+1
//         } 
//         else if (choose == 2) 
//         {
//             // 查询操作：统计符合条件的记录数
//             int x, ymin, ymax;
//             scanf("%d %d %d", &x, &ymin, &ymax);
//             int count = 0;

//             // 遍历所有已插入的记录
//             for (int i = 0; i < record_count; i++) 
//             {
//                 int val = records[i][x];
//                 // 条件：1.该字段有值（val≠0）；2.值在[ymin, ymax]
//                 if (val != 0 && val >= ymin && val <= ymax) 
//                 {
//                     count++;
//                 }
//             }

//             printf("%d\n", count);
//         }
//     }


//     return 0;
// }