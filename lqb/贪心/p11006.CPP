// #include <bits/stdc++.h>
// using namespace std;

// #define endl '\n'
// using ll = long long;

// int main()
// {
//     ios::sync_with_stdio(false);
//     cin.tie(nullptr);

//     int T = 1;
//     cin>>T;

//     while (T--)
//     {
//         // 最坏鸽笼原理

//         /*
//             本题本质是抽屉原理的逆向应用，通过贪心构造最坏情况下的士兵分布来寻找答案的下界。

//             本题原意是要找“确保组成k个小组的最小士兵数”，“确保”就意味着我们要找最坏情况。
//             转化成逆命题即为：
//             求最大的 y 使得存在一种选出 y 名士兵的方案，在这 y 名士兵中无法选出 k 个种类相同的三人组。
//             那么选出 (y+1) 名士兵时就一定满足题意，所以最小的 x 即为 y+1。

//             我们定义小于 3 的士兵为阻碍（这些士兵凑不出完整的小组），定义大于等于 3 的为成组。
//             因为我们要的是最劣状态，所以每种士兵都会先拿出阻碍。

//             接下来就只能“被迫”的构成小组了。发现剩余的可以分为三种：
//                 大于等于 3 的，这种在拿 1 个就能换取 2 个阻碍。
//                 2 个的，这种再拿只能换取 1 个阻碍。
//                 1 个的，这种换不来阻碍。
//             利用贪心最劣策略，在限制小组数量的前提下，优先选择“贡献阻碍”最多的职业进行填充。

//             本题的数据范围很大，而 n 只有 2×10^5，所以考虑用 map 来统计。
//             通过贪心求得的只是在凑齐 k 组前能取得的最大士兵数，
//             res+1 代表了从“无法凑齐 k 组的最大人数”向“必然凑齐 k 组”的跨越。
        
//         */

//         int n;
//         ll k;
//         cin>>n>>k;

//         unordered_map<int, ll> mp;
//         for (int i = 0; i < n; i++)
//         {
//             int a, b;
//             cin>>a>>b;

//             mp[a] += b;
//         }

//         ll tot = 0, w = 0;
//         ll f = 0;
//         ll ex[3] = {0, 0, 0};
//         for (auto it = mp.begin(); it != mp.end(); it++)
//         {
//             auto b = it->second;

//             // 能组成的完整的三人组数
//             tot += b / 3;
//             if (b <= 2)
//             {
//                 // 无法组成三人组，则全部取出（障碍）
//                 w += b;
//             }
//             else
//             {
//                 // 能组成三人组，则先取 2个（障碍）
//                 w += 2;
//                 // 可组成三人组的数量
//                 f += (b - 2) / 3;
//                 ex[(b - 2) % 3]++;
//             }
//         }

//         // 完整的三人组数 不足 k 个，无解
//         if (tot < k)
//         {
//             cout<<-1<<endl;
//             continue;
//         }

//         ll tar = k - 1;
//         // 已选出的士兵总数
//         ll ans = w;
//         ll t = min(f, tar);

//         ans += t * 3;
//         tar -= t;

//         // 处理余数士兵
//         for (int i = 2; i >= 1; i--)
//         {
//             ll tf = min(ex[i], tar);
//             ans += tf * i;
//             tar -= tf;
//         }

//         // 最小 x = y + 1
//         cout<<ans + 1<<endl;
//     }

//     return 0;
// }