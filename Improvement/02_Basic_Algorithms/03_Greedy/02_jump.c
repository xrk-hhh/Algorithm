// #include <stdio.h>
// #include <stdlib.h>

// // 升序排序的比较函数（更安全的写法，避免int溢出）
// int cmp(const void *a, const void *b) 
// {
//     int num1 = *(const int*)a;
//     int num2 = *(const int*)b;
//     if (num1 < num2)
//     {
//         return -1;
//     }
//     if (num1 > num2)
//     {
//         return 1;
//     }
//     return 0;
// }

// int main()
// {
//     /*
//         需求：跳跳！
//         题目描述：
//         你是一只小跳蛙，你特别擅长在各种地方跳来跳去。

//         这一天，你和朋友小 F 一起出去玩耍的时候，遇到了一堆高矮不同的石头，其中第 i 块的石头高度为 hi，地面的高度是 h0=0。
//         你估计着，从第 i 块石头跳到第 j 块石头上耗费的体力值为 (hi - hj)²，
//         从地面跳到第 i 块石头耗费的体力值是 (hi)²。

//         为了给小 F 展现你超级跳的本领，你决定跳到每个石头上各一次，并最终停在任意一块石头上，并且小跳蛙想耗费尽可能多的体力值。

//         当然，你只是一只小跳蛙，你只会跳，不知道怎么跳才能让本领更充分地展现。

//         不过你有救啦！小 F 给你递来了一个写着 AK 的电脑，你可以使用计算机程序帮你解决这个问题，万能的计算机会告诉你怎么跳。

//         那就请你——会写代码的小跳蛙——写下这个程序，为你 NOIp AK 踏出坚实的一步吧！

//         输入格式：
//         输入一行一个正整数 n，表示石头个数。

//         输入第二行 n 个正整数，表示第 i 块石头的高度 hi。

//         输出格式：
//         输出一行一个正整数，表示你可以耗费的体力值的最大值。

//         说明/提示：
//         数据范围
//         对于 1≤i≤n，有 0<hi≤10^4，且保证 hi 互不相同。

//         对于 10% 的数据，n≤3；

//         对于 20% 的数据，n≤10；

//         对于 50% 的数据，n≤20；

//         对于 80% 的数据，n≤50；

//         对于 100% 的数据，n≤300。
//     */

//     int n;
//     scanf("%d", &n);

//     int h[310];
//     // h[0]是地面，高度0
//     h[0] = 0;
//     for (int i = 1; i <= n; i++)
//     {
//         scanf("%d", &h[i]);
//     }

//     // 排序：地面+所有石头升序排列
//     qsort(h, n + 1, sizeof(int),cmp);

    
//     long long sum = 0;
//     int h_res[310];
//     h_res[0] = 0;

//     // 方法一： “奇偶交替取最大 / 最小” 
//     // int j = 0;
//     // int k = 1;
//     // for (int i = 1; i <= n; i++)
//     // {
//     //     if (i % 2 != 0)
//     //     {
//     //         h_res[i] = h[n - j];
//     //         j++;
//     //     }
//     //     else
//     //     {
//     //         h_res[i] = h[k];
//     //         k++;
//     //     }
//     //      // 计算本次跳跃的体力：先转long long避免溢出
//     //     long long diff = h_res[i] - h_res[i - 1];
//     //     sum += diff * diff;
//     // }

//     // 方法二：双指针实现贪心策略：交替取最大、最小、次大、次小...
//     int left = 1;   // 最小石头的索引（排序后h[1]是最小）
//     int right = n;  // 最大石头的索引（排序后h[n]是最大）
//     int flag = 1;   // 1：取右指针（最大），0：取左指针（最小）
//     for (int i = 1; i <= n; i++) 
//     {
//         if (flag) 
//         {
//             h_res[i] = h[right];
//             right--;
//         } 
//         else
//         {
//             h_res[i] = h[left];
//             left++;
//         }
//         flag = !flag; // 切换取数方向

//         // 计算本次跳跃的体力：先转long long避免溢出
//         long long diff = h_res[i] - h_res[i - 1];
//         sum += diff * diff;
//     }

//     printf("%lld\n",sum);

//     return 0;
// }