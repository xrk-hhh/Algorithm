// #include <stdio.h>

// int countTwo(int x);

// int main()
// {
//     /*
//         需求：数字统计
//         题目描述：
//         请统计某个给定范围 [L,R] 的所有整数中，数字 2 出现的次数。

//         比如给定范围 [2,22]，数字 2 在数 2 中出现了 1 次，在数 12 中出现 1 次，在数 20 中出现 1 次，在数 21 中出现 1 次，
//         在数 22 中出现 2 次，所以数字 2 在该范围内一共出现了 6 次。

//         输入格式：
//         2 个正整数 L 和 R，之间用一个空格隔开。

//         输出格式：
//         数字 2 出现的次数。

//         说明/提示：
//         1≤L≤R≤100000。

//     */

//     /*
//         暴力法
//         遍历[L, R]中的每个数字，对每个数字逐位判断是否为 2。
//         时间复杂度：O((R-L+1) × d)，其中d是数字的最大位数（如100000是 6 位）。
//         当R-L很大时（比如L=1，R=1e5），需要处理 10 万个数字，每个数字最多检查 6 位，总操作约 60 万次，
//         虽然能通过本题，但扩展性差（若范围扩大到1e9，操作次数会达 9 亿次，效率极低）。
//     */
//     // int L, R;
//     // scanf("%d %d",&L, &R);

//     // int count = 0;
//     // for (int i = L; i <= R; i++)
//     // {
//     //     int num = i;
//     //     while (num != 0)
//     //     {
//     //         int temp = num % 10;
//     //         num /= 10;
//     //         if (temp == 2)
//     //         {
//     //             count++;
//     //         }
//     //     }
//     // }

//     // printf("%d",count);

//     /*
//         数学法
//         直接通过数学规律计算 “0 到 x 中数字 2 出现的总次数”，再用count(R) - count(L-1)得到[L, R]的结果。
//         时间复杂度：O(d)，仅与数字的位数d有关（最多 6 位），无论范围多大（即使1e9），只需计算 6 次，效率极大提升。
//     */
//     int L, R;
//     scanf("%d %d", &L, &R);
//     // [L, R]的结果 = 0~R的次数 - 0~(L-1)的次数
//     int result = countTwo(R) - countTwo(L - 1);
//     printf("%d\n", result);

//     return 0;
// }

// // 计算0到x中数字2出现的总次数
// int countTwo(int x)
// {
//     int ans = 0;
//     // i表示当前分析的数位（1：个位，10：十位，100：百位...）
//     for (int i = 1; i <= x; i *= 10)
//     {
//         int a = x / i; // 高位部分（当前位及以上）
//         int b = x % i; // 低位部分（当前位以下）
//         // 计算当前数位上2出现的次数
//         // 若此处为1，则是(a + 8) / 10 * i
//         // 总结：(a + k) / 10 * i
//         ans += (a + 7) / 10 * i; // 基础次数（覆盖当前位<2或>2的情况）
//         if (a % 10 == 2)
//         { // 若当前位恰好为2，补充低位的次数
//             ans += b + 1;
//         }
//     }
//     return ans;
// }