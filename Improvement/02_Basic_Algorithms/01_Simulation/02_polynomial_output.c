// #include <stdio.h>
// #include <stdlib.h>

// int main()
// {
//     /*
//         需求：多项式输出
//         题目描述：
//         一元 n 次多项式可用如下的表达式表示：

//         f(x)=anx^n + an−1x^n−1 + ⋯ + a1x + a0, an≠0
//         其中，ai^xi称为 i 次项，ai称为 i 次项的系数。给出一个一元多项式各项的次数和系数，请按照如下规定的格式要求输出该多项式：

//         多项式中自变量为 x，从左到右按照次数递减顺序给出多项式。

//         多项式中只包含系数不为 0 的项。

//         如果多项式 n 次项系数为正，则多项式开头不出 + 号，如果多项式 +n 次项系数为负，则多项式以 - 号开头。

//         对于不是最高次的项，以 + 号或者 - 号连接此项与前一项，分别表示此项系数为正或者系数为负。
//         紧跟一个正整数，表示此项系数的绝对值（如果一个高于 +-0 次的项，其系数的绝对值为 1，则无需输出 1）。
//         如果 x 的指数大于 1，则接下来紧跟的指数部分的形式为“x^b”，其中 b 为 x 的指数；
//         如果 x 的指数为 1，则接下来紧跟的指数部分形式为 x；如果 x 的指数为 0，则仅需输出系数即可。

//         多项式中，多项式的开头、结尾不含多余的空格。

//         输入格式：
//         输入共有 2 行。

//         第一行 1 个整数，n，表示一元多项式的次数。
//         第二行有 n+1 个整数，其中第 i 个整数表示第 n−i+1 次项的系数，每两个整数之间用空格隔开。

//         输出格式：
//         输出共 1 行，按题目所述格式输出多项式。

//         说明/提示：
//         对于100%数据，0≤n≤100，−100≤系数≤100

//     */

//     int n;
//     scanf("%d", &n);

//     // 存储系数：coeff[i] 对应 次数为 (n - i) 的项的系数（i从0到n）
//     int coeff[101] = {0}; // n≤100，数组大小设101足够
//     for (int i = 0; i <= n; i++) 
//     {
//         scanf("%d", &coeff[i]);
//     }

//     int is_first = 1; // 标记是否是第一个非0项（处理开头符号）
//     int has_term = 0; // 标记是否有非0项（处理全0情况）

//     // 遍历所有项（按次数从高到低：i=0对应最高次n，i=n对应0次）
//     for (int i = 0; i <= n; i++) 
//     {
//         int a = coeff[i];       // 当前项的系数
//         int exp = n - i;        // 当前项的指数（次数）
//         if (a == 0)
//         {
//             continue;  // 跳过系数为0的项
//         }
//         has_term = 1;           // 标记存在非0项

//         // ------------- 第一步：处理符号 -------------
//         if (is_first) 
//         {
//             // 第一个非0项：负系数输出'-'，正系数不输出'+'
//             if (a < 0) 
//             {
//                 printf("-");
//             }
//             is_first = 0; // 第一个项处理完，后续都是非第一个
//         }
//         else 
//         {
//             // 非第一个非0项：正系数输出'+'，负系数输出'-'
//             if (a > 0) 
//             {
//                 printf("+");
//             } 
//             else 
//             {
//                 printf("-");
//             }
//         }

//         // ------------- 第二步：处理系数（取绝对值）-------------
//         int abs_a = abs(a);
//         // 只有“次数>0 且 系数绝对值=1”时，不输出1；其他情况输出绝对值
//         if (!(exp > 0 && abs_a == 1)) 
//         {
//             printf("%d", abs_a);
//         }

//         // ------------- 第三步：处理指数（x的部分）-------------
//         if (exp > 1) 
//         {          // 指数>1：输出x^exp
//             printf("x^%d", exp);
//         } 
//         else if (exp == 1) 
//         {  // 指数=1：仅输出x
//             printf("x");
//         }
//         // 指数=0：无x部分（无需处理）
//     }

//     // 特殊情况：所有项系数都是0 → 输出0
//     if (!has_term) 
//     {
//         printf("0");
//     }

//     printf("\n");

//     return 0;
// }