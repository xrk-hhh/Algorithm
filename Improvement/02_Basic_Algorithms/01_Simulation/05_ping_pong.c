// #include <stdio.h>
// #include <stdlib.h> // 用于abs函数（计算分差绝对值）

// void process(char c, int threshold, int *w, int *l, char res[][20], int *cnt);

// int main()
// {
//     /*
//         需求：乒乓球
//         题目背景：
//         国际乒联现在主席沙拉拉自从上任以来就立志于推行一系列改革，以推动乒乓球运动在全球的普及。
//         其中 11 分制改革引起了很大的争议，有一部分球员因为无法适应新规则只能选择退役。
//         华华就是其中一位，他退役之后走上了乒乓球研究工作，意图弄明白 11 分制和 21 分制对选手的不同影响。
//         在开展他的研究之前，他首先需要对他多年比赛的统计数据进行一些分析，所以需要你的帮忙。

//         题目描述：
//         华华通过以下方式进行分析，首先将比赛每个球的胜负列成一张表，然后分别计算在 11 分制和 21 分制下，双方的比赛结果（截至记录末尾）。

//         比如现在有这么一份记录，（其中 W 表示华华获得一分，L 表示华华对手获得一分）：

//         WWWWWWWWWWWWWWWWWWWWWWLW
//         在 11 分制下，此时比赛的结果是华华第一局 11 比 0 获胜，第二局 11 比 0 获胜，正在进行第三局，当前比分 1 比 1。
//         而在 21 分制下，此时比赛结果是华华第一局 21 比 0 获胜，正在进行第二局，比分 2 比 1。
//         如果一局比赛刚开始，则此时比分为 0 比 0。直到分差大于或者等于 2，才一局结束。

//         注意：当一局比赛结束后，下一局立刻开始。

//         你的程序就是要对于一系列比赛信息的输入（WL 形式），输出正确的结果。

//         输入格式：
//         每个输入文件包含若干行字符串，字符串由大写的 W 、 L 和 E 组成。
//         其中 E 表示比赛信息结束，程序应该忽略 E 之后的所有内容。

//         输出格式：
//         输出由两部分组成，每部分有若干行，每一行对应一局比赛的比分（按比赛信息输入顺序）
//         。其中第一部分是 11 分制下的结果，第二部分是 21 分制下的结果，两部分之间由一个空行分隔。

//         说明/提示：
//         每行至多 25 个字母，最多有 2500 行。

//         【管理员附注】
//         本题为非常早期的试题，在测试点中出现了如下两个问题。在洛谷上这两个测试点的疏漏被保留：

//         有一个测试点实际上有 2501 行数据。
//         有一个测试点的输入数据出现了非 W 、 L 和 E 的字符，不符合输入格式的要求。
//         不过这些字符只出现在 E 字符之后，按题目要求忽略 E 之后的全部字符即可。
//     */

//     // 11分制：存储已结束+当前局的比分，cnt11是总局数
//     char res11[6000][20];
//     int cnt11 = 0;
//     int w11 = 0, l11 = 0; // 当前局：华华得分、对手得分

//     // 21分制：存储已结束+当前局的比分，cnt21是总局数
//     char res21[6000][20];
//     int cnt21 = 0;
//     int w21 = 0, l21 = 0; // 当前局：华华得分、对手得分

//     int c;
//     while ((c = fgetc(stdin)) != EOF)
//     {
//         // 遇到'E'立即停止处理后续内容
//         if (c == 'E')
//         {
//             break;
//         }
//         // 忽略非W/L的字符（如换行、空格等）
//         if (c != 'W' && c != 'L')
//         {
//             continue;
//         }

//         // 调用函数处理11分制（阈值11）
//         process(c, 11, &w11, &l11, res11, &cnt11);
//         // 调用函数处理21分制（阈值21）
//         process(c, 21, &w21, &l21, res21, &cnt21);
//     }

//     // ----------------- 当前未结束的局 -----------------
//     // 11分制的当前局（无论是否结束，都要输出）
//     sprintf(res11[cnt11], "%d:%d", w11, l11);
//     cnt11++;
//     // 21分制的当前局
//     sprintf(res21[cnt21], "%d:%d", w21, l21);
//     cnt21++;

//     // ----------------- 输出结果 -----------------
//     // 输出11分制所有局
//     for (int i = 0; i < cnt11; i++)
//     {
//         printf("%s\n", res11[i]);
//     }
//     // 两部分之间空一行
//     printf("\n");
//     // 输出21分制所有局
//     for (int i = 0; i < cnt21; i++)
//     {
//         printf("%s\n", res21[i]);
//     }

//     return 0;
// }

// // 分制处理函数：处理单字符并更新状态
// // 参数：
// // - c: 当前字符（W/L）
// // - threshold: 分制阈值（11或21）
// // - w: 华华当前局得分（指针，需修改）
// // - l: 对手当前局得分（指针）
// // - res: 存储已结束局比分的数组
// // - cnt: 已结束局数（指针）
// void process(char c, int threshold, int *w, int *l, char res[][20], int *cnt)
// {
//     // 更新当前局得分
//     if (c == 'W')
//     {
//         (*w)++;
//     }
//     else
//     {
//         (*l)++;
//     }

//     // 判断当前局是否结束
//     if ((*w >= threshold || *l >= threshold) && abs(*w - *l) >= 2)
//     {
//         // 记录本局比分
//         sprintf(res[*cnt], "%d:%d", *w, *l);
//         (*cnt)++;
//         // 重置下一局得分
//         *w = 0;
//         *l = 0;
//     }
// }