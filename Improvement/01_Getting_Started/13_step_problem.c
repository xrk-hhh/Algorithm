// #include <stdio.h>

// #define MOD 100003   // 题目要求的模数
// #define MAXN 100005 // 最大 N 为 1e5+5，避免越界

// int main()
// {
//     /*
//         需求：台阶问题
//         题目描述：
//         有 N 级台阶，你一开始在底部，每次可以向上迈 1∼K 级台阶，问到达第 N 级台阶有多少种不同方式。

//         输入格式：
//         两个正整数 N,K。

//         输出格式：
//         一个正整数 ans(mod100003)，为到达第 N 级台阶的不同方式数。
    
//         说明/提示：
//         对于 20% 的数据，1≤N≤10，1≤K≤3；
//         对于 40% 的数据，1≤N≤1000；
//         对于 100% 的数据，1≤N≤100000，1≤K≤100。
//     */

//     int N, K;
//     scanf("%d %d",&N, &K);

//     // 方法一：dp
//     /*
//         核心思路
//         状态定义：设dp[i]表示到达第i级台阶的不同方式数。

//         状态转移：
//         每次可走1~K级台阶，因此到达第i级的方式数 = 到达i-1级的方式数 + 到达i-2级的方式数 + … + 到达i-K级的方式数（若i-K ≥ 0）。
//         即：dp[i] = dp[i-1] + dp[i-2] + ... + dp[i-K]（需取模100003）。

//         前缀和优化：直接计算dp[i]需遍历K项（时间复杂度O(N*K)），用前缀和sum[i] = dp[0]+dp[1]+...+dp[i]，可将dp[i]的计算优化为O(1)：
//         若i ≤ K：dp[i] = sum[i-1]（前i-1项和）；
//         若i > K：dp[i] = (sum[i-1] - sum[i-K-1]) % MOD（减去超出K项的部分）。

//         初始条件：dp[0] = 1（地面视为第 0 级，有一种 “到达” 方式）。
    
//     */

//     // long long dp[MAXN] = {0};   // 存储到达第i级的方式数
//     // long long sum[MAXN] = {0};  // 前缀和数组：sum[i] = dp[0]+...+dp[i]
    
//     // dp[0] = 1;                  // 初始条件：地面有1种方式
//     // sum[0] = dp[0];             // 前缀和初始化
    
//     // for (int i = 1; i <= N; i++) 
//     // {
//     //     if (i <= K) 
//     //     {
//     //         // i ≤ K时，前 i 项和就是 sum[i-1]
//     //         dp[i] = sum[i-1];
//     //     } 
//     //     else 
//     //     {
//     //         // i > K时，减去超出 K 项的部分（加MOD避免负数）
//     //         dp[i] = (sum[i-1] - sum[i-K-1] + MOD) % MOD;
//     //     }
//     //     sum[i] = (sum[i-1] + dp[i]) % MOD; // 更新前缀和
//     // }
    
//     // printf("%lld\n", dp[N] % MOD); // 输出结果（确保取模）

//     // 方法二：找规律
//     /*
//         1.a[x]=a[x-1]+a[x-2]+..+a[x-k]

//         2.a[x+1]=a[x]+a[x-1]+a[x-2]+…+a[x+1-k]=2*a[x]-a[x-k]


//         规律： 
//         当n<=k时,第N项=(上一项*2)%100003;
        
//         当n>k时 ,第N项=(上一项*2-第n-1-k项)%100003;
//     */

//     int a[MAXN]; // 数组大小足够
//     a[0] = 1;
//     a[1] = 1;
//     for(int i = 2; i <= N; i++)
//     {
//         if(i <= K)
//         {
//             a[i] = (a[i-1] * 2) % MOD;
//         }
//         else
//         {
//             a[i] = (a[i-1] * 2 - a[i-1-K]) % MOD;
//         }
//     }
//     printf("%d",(a[N] + MOD) % MOD); // 确保结果非负

//     return 0;
// }