// #include <stdio.h>
// #include <string.h>

// void toLower(char *str);

// int main()
// {
//     /*
//         需求：统计单词数
//         题目描述：
//         一般的文本编辑器都有查找单词的功能，该功能可以快速定位特定单词在文章中的位置，有的还能统计出特定单词在文章中出现的次数。

//         现在，请你编程实现这一功能，具体要求是：给定一个单词，请你输出它在给定的文章中出现的次数和第一次出现的位置。
//         注意：匹配单词时，不区分大小写，但要求完全匹配，即给定单词必须与文章中的某一独立单词在不区分大小写的情况下完全相同（参见样例 1），
//         如果给定单词仅是文章中某一单词的一部分则不算匹配（参见样例 2）。

//         输入格式：
//         共 2 行。

//         第 1 行为一个字符串，其中只含字母，表示给定单词；

//         第 2 行为一个字符串，其中只可能包含字母和空格，表示给定的文章。

//         输出格式：
//         一行，如果在文章中找到给定单词则输出两个整数，两个整数之间用一个空格隔开，
//         分别是单词在文章中出现的次数和第一次出现的位置（即在文章中第一次出现时，单词首字母在文章中的位置，位置从 0 开始）；
//         如果单词在文章中没有出现，则直接输出一个整数 −1。

//         注意：空格占一个字母位

//         说明/提示：
//         数据范围
//         1≤ 第一行单词长度 ≤10。
//         1≤ 文章长度 ≤10^6。
//     */

//     // 目标单词（最长10字符）
//     char target[11];
//     // 文章（最长1e6字符）
//     char article[1000001];

//     // 读取目标单词，并清除缓冲区的换行符
//     scanf("%s",target);
//     // 吃掉scanf留下的换行符，避免fgets误读
//     getchar();

//     // 读取文章（包含空格）
//     fgets(article,1000001,stdin);

//     // 统一转换为小写（不区分大小写匹配）
//     toLower(target);
//     toLower(article);

//     // 目标单词长度
//     int len_target = strlen(target);
//     int len_article = strlen(article);
//     // 出现次数
//     int count = 0;
//     // 首次出现位置（初始为-1）
//     int first_pos = -1;
    
//      // 遍历文章，寻找所有独立单词
//     for (int i = 0; i < len_article; i++) 
//     {
//         // 定位独立单词的起始位置：
//         // 要么是文章开头且当前不是空格，要么前一个字符是空格且当前不是空格
//         if ((i == 0 || article[i-1] == ' ') && article[i] != ' ') 
//         {
//             int start = i; // 单词起始索引
//             int end = start;

//             // 找到单词结束位置（遇到空格或文章结尾）
//             while (end < len_article && article[end] != ' ') 
//             {
//                 end++;
//             }
//             int len_word = end - start; // 当前单词长度

//             // 长度不同，直接跳过（不可能匹配）
//             if (len_word != len_target) 
//             {
//                 i = end - 1; // 跳过当前单词，避免重复检查
//                 continue;
//             }

//             // 长度相同，逐字符匹配
//             int match = 1;
//             for (int k = 0; k < len_target; k++) 
//             {
//                 if (article[start + k] != target[k]) 
//                 {
//                     match = 0;
//                     break;
//                 }
//             }

//             // 匹配成功
//             if (match) 
//             {
//                 count++;
//                 if (first_pos == -1) 
//                 { // 记录首次出现位置
//                     first_pos = start;
//                 }
//             }

//             i = end - 1; // 跳过当前单词，继续下一个
//         }
//     }

//     // 输出结果
//     if (count > 0) 
//     {
//         printf("%d %d\n", count, first_pos);
//     } 
//     else 
//     {
//         printf("-1\n");
//     }

//     return 0;
// }

// void toLower(char *str)
// {
//     for (int i = 0; str[i] != '\0'; i++)
//     {
//         if (str[i] >= 'A' && str[i] <= 'Z')
//         {
//             str[i] += 32;
//         }
//     }
// }