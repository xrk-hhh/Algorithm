// #include <stdio.h>

// #define MAX_N 1005

// int main()
// {
//     /*
//         需求：数的计算
//         题目描述：
//         给出正整数 n，要求按如下方式构造数列：

//         只有一个数 n 的数列是一个合法的数列。
//         在一个合法的数列的末尾加入一个正整数，但是这个正整数不能超过该数列最后一项的一半，可以得到一个新的合法数列。
//         请你求出，一共有多少个合法的数列。两个合法数列 a,b 不同当且仅当两数列长度不同或存在一个正整数 i≤∣a∣，使得 ai≠bi。

//         输入格式：
//         输入只有一行一个整数，表示 n。

//         输出格式：
//         输出一行一个整数，表示合法的数列个数。

//         说明/提示：
//         数据规模与约定
//         对于全部的测试点，保证 1≤n≤10^3。
    
//     */

//     int n;
//     scanf("%d",&n);
    
//     int dp[MAX_N] = {0};
//     int pre_sum[MAX_N] = {0}; // pre_sum[i] = dp[1]+dp[2]+...+dp[i]
    
//     dp[1] = 1;
//     pre_sum[1] = 1;
    
//     /*
//         以 i 为最后一项的数列 = [i]（自身） + 所有 “以 i 结尾并追加合法数 k 形成的新数列”（k 满足 1≤k≤i/2）。
//         对应到 dp[i]：
//         首先，dp[i] 至少包含 [i] 这 1 个数列，所以初始值为 1；
//         其次，对于每个合法的 k（1≤k≤i/2），以 k 为最后一项的合法数列（共 dp[k] 个）都可以接在 i 后面，
//         形成新的合法数列（比如 [k] 接在 i 后变成 [i,k]，[k,1] 接在 i 后变成 [i,k,1]）。
//         优化前状态转移方程：
//         dp[i] = 1 + dp[1] + dp[2] + ... + dp[i//2]

//         为了加速，我们引入前缀和数组 pre_sum：
//         定义 pre_sum[x] = dp[1] + dp[2] + ... + dp[x]（即前 x 项 dp 的和）；
//         这样，dp[1]+dp[2]+...+dp[i//2] 就等于 pre_sum[i//2]，无需循环累加；
//         同时，pre_sum[i] = pre_sum[i-1] + dp[i]（每次计算完 dp[i] 后，更新前缀和）。

//         优化后的状态转移方程：
//         dp[i] = 1 + pre_sum[i//2]
//     */
//     for (int i = 2; i <= n; i++) 
//     {
//         dp[i] = 1 + pre_sum[i / 2]; // 直接用前缀和，无需循环累加
//         pre_sum[i] = pre_sum[i - 1] + dp[i]; // 更新前缀和
//     }
    
//     printf("%d\n", dp[n]);

//     return 0;
// }

