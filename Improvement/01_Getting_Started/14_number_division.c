// #include <stdio.h>

// // dp[i][j]：将i分成j份的分法数
// int dp[205][7] = {0}; // n≤200，k≤6，数组大小足够

// int main()
// {
//     /*
//         需求：数的划分
//         题目描述：
//         将整数 n 分成 k 份，且每份不能为空，任意两个方案不相同（不考虑顺序）。

//         例如：n=7，k=3，下面三种分法被认为是相同的。

//         1,1,5;
//         1,5,1;
//         5,1,1.

//         问有多少种不同的分法。

//         输入格式：
//         n,k （6<n≤200，2≤k≤6）。

//         输出格式：
//         1 个整数，即不同的分法。

//     */

//     int n, k;
//     scanf("%d %d",&n, &k);

//     /*
//         先明确状态定义
//         dp[i][j]：将整数i分成j份（每份不能为空、不考虑顺序）的不同分法数。

//         分情况讨论（核心推导）
//         要把i分成j份（每份非空、无顺序），所有分法可以分为两类：

//         情况 1：分法中至少有一份是 1
//         因为不考虑顺序，我们可以固定其中一份为1，那么剩下的任务就是：
//         把i-1分成j-1份（剩下的i-1需要凑够j-1份，每份仍非空）。

//         情况 2：分法中每份都大于 1
//         因为每份都大于 1，我们可以给每一份都减去 1（保证每份仍非空），此时：
//         总数从i变成i-j（j份各减 1，总共减j）；
//         份数仍然是j份（每份减 1 后还是非空）；

//         合并两种情况
//         因为 “有至少一份是 1” 和 “每份都大于 1” 是互斥的（不可能同时满足），
//         且覆盖了所有合法分法（任何分法要么有 1，要么没有 1），
//         所以：dp[i][j] = 情况1的分法数 + 情况2的分法数 = dp[i-1][j-1] + dp[i-j][j]

//         补充边界条件（推导的前提）
//         当i < j时：数不够分（j份至少需要j，而i<j），所以dp[i][j] = 0；
//         当j=1时：任何数分成 1 份只有 1 种分法，所以dp[i][1] = 1；
//         当i=j时：只能分成j个 1，所以dp[i][j] = 1（对应dp[i-j][j] = dp[0][j]，可视为 1）。
    
//     */
//     // 初始条件：分成1份只有1种分法
//     for (int i = 1; i <= n; i++) 
//     {
//         dp[i][1] = 1;
//     }

//     // 动态规划递推
//     for (int j = 2; j <= k; j++) 
//     { // 枚举份数j
//         for (int i = j; i <= n; i++) 
//         { // i至少为j（每份至少1）
//             dp[i][j] = dp[i-1][j-1] + dp[i-j][j];
//         }
//     }

//     printf("%d\n", dp[n][k]);

//     return 0;
// }