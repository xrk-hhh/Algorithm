// #include <stdio.h>
// #include <string.h>

// int main()
// {
//     /*
//         需求：珠心算测验
//         题目描述：
//         X 校正在进行 CSP 前的校内集训。

//         一共有 n 名 OIer 参与这次集训，教练为他们精心准备了 m 套模拟赛题。

//         然而，每名 OIer 都有各自的时间安排，巧合的是，他们在接下来的 k 天中都恰好有 m 天有空打模拟赛。

//         为了方便管理，教练规定一个人必须按顺序打完 m 套模拟赛题。

//         比如，小 X 在接下来的第 2,3,5 天有空打模拟赛，那么他就必须在第 2 天打第 1 套模拟赛题，第 3 天打第 2 套模拟赛题，第 5 天打第 3 套模拟赛题。

//         教练需要为每一个人的每一次模拟赛做准备，为了减小工作量，如果在某一天有多个人打同一套模拟赛题，那么教练只需要在这一天准备一场使用这一套题的模拟赛即可。

//         你作为机房大佬，教练想请你帮他计算一下，他每天需要准备多少场模拟赛。

//         输入格式：
//         第一行三个整数 n,m,k。

//         接下来 n 行，每行 m 个整数，第 i 行第 j 列的整数 ai,j 表示第 i 个人在接下来的 k 天中第 j 个有空的日子为第 ai,j 天。

//         输出格式：
//         一行 k 个整数，第 i 个整数表示接下来的第 i 天教练需要准备的模拟赛场数。

//         说明/提示：
//         本题采用捆绑测试。

//         Subtask 1（13 points）：n=m=k=1。
//         Subtask 2（24 points）：n=1。
//         Subtask 3（24 points）：m=1。
//         Subtask 4（39 points）：无特殊限制。
//         对于 100% 的数据，1≤n,m,k≤10^3，m≤k，1≤ai,1<ai,2<⋯<ai,m≤k。
//     */

//     int n, m, k;
//     scanf("%d %d %d",&n,&m,&k);

//     // 存储每个学生的m个空闲日（第i个学生的第j个空闲日）
//     int day[n][m];
//     for (int i = 0; i < n; i++)
//     {
//         for (int j = 0; j < m; j++)
//         {
//             scanf("%d",&day[i][j]);
//         }
//     }

//     // 方法一：根据day对cnt进行标记，再根据标记统计并输出，运行时间短，但所需内存多
//     // cnt[d][t] = 1 表示第d天有第t套题被安排（d:1~k, t:1~m）
//     int cnt[k + 1][m + 1];  // 初始化为0
//     memset(cnt,0,sizeof(cnt));

//     // 标记所有安排：每个学生的第j个空闲日对应第j+1套题（j是0-based）
//     for (int i = 0; i < n; i++) 
//     {
//         for (int j = 0; j < m; j++) 
//         {
//             int d = day[i][j];  // 当天
//             int t = j + 1;      // 对应的套题编号（1-based）
//             cnt[d][t] = 1;      // 标记该天该套题有安排（重复标记不影响）
//         }
//     }

//     // 计算每天需要准备的场数（不同套题的数量）
//     for (int d = 1; d <= k; d++) 
//     {
//         int total = 0;
//         for (int t = 1; t <= m; t++) 
//         {
//             total += cnt[d][t];  // 累加当天被安排的套题数量
//         }
//         // 控制输出格式（相邻天数用空格分隔）
//         if (d > 1) 
//         {
//             printf(" ");
//         }
//         printf("%d", total);
//     }

//     // // 方法二：改用临时标记数组记录当天已统计过的套题，所需内存少，但运行时间长
//     // // res[d]：第d天需要准备的场次（1-based）
//     // int res[k + 1];
//     // // 初始化为0
//     // memset(res,0,sizeof(res));
//     // // 临时标记数组：记录当天已统计过的套题（避免重复计数）
//     // int temp[m + 1];  // 大小为m+1（套题编号1~m）

//     // // 遍历每一天d（1~k）
//     // for (int d = 1; d <= k; d++) 
//     // {
//     //     // 初始化临时标记数组为0（表示当天所有套题未统计）
//     //     memset(temp, 0, sizeof(temp));
        
//     //     // 遍历所有学生，检查是否在第d天做某套题
//     //     for (int i = 0; i < n; i++) 
//     //     {
//     //         for (int j = 0; j < m; j++) 
//     //         {
//     //             // 若当前学生的第j个空闲日是d，对应套题t = j+1
//     //             if (day[i][j] == d) 
//     //             {
//     //                 int t = j + 1;
//     //                 // 若该套题当天未统计过，则计数+1并标记
//     //                 if (temp[t] == 0) 
//     //                 {
//     //                     res[d]++;
//     //                     temp[t] = 1;  // 标记为已统计，避免重复
//     //                 }
//     //             }
//     //         }
//     //     }
//     // }

//     // // 输出结果
//     // for (int d = 1; d <= k; d++) 
//     // {
//     //     if (d > 1)
//     //     {
//     //         printf(" ");
//     //     } 
//     //     printf("%d", res[d]);
//     // }

//     return 0;
// }