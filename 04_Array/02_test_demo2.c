// #include <stdio.h>

// int main()
// {
//     /*
//         需求：小鱼的数字游戏
//         题目描述：
//         小鱼最近被要求参加一个数字游戏，要求它把看到的一串数字 ai（长度不一定，以 0 结束），记住了然后反着念出来（表示结束的数字 0 就不要念出来了）。
//         这对小鱼的那点记忆力来说实在是太难了，你也不想想小鱼的整个脑袋才多大，其中一部分还是好吃的肉！
//         所以请你帮小鱼编程解决这个问题。

//         输入格式：
//         一行内输入一串整数，以 0 结束，以空格间隔。

//         输出格式：
//         一行内倒着输出这一串整数，以空格间隔。

//         说明/提示:
//         数据规模与约定
//         对于 100% 的数据，保证 0≤ai≤(2^31)−1，数字个数不超过 100。
//     */

//     int num[100];
//     // 记录有效数字的个数
//     int i = 0;
//     // 临时存储读取的数字
//     int temp;
//     // 读取输入，遇到0则终止（0不存入数组）
//     while (1)
//     {
//         scanf("%d",&temp);
//         // 0是终止符，不存入数组
//         if (temp == 0)
//         {
//             break;
//         }
//         // 非0数字存入数组
//         num[i] = temp;
//         i++;
//     }
//      // 倒序输出数组中的数字（空格分隔，最后无多余空格）
//     for (int j = i - 1; j >= 0; j--)
//     {
//         printf("%d",num[j]);
//         // 除第一个输出的数字（原最后一个）外，其他后加空格
//         if (j != 0)
//         {
//             printf(" ");
//         }
//     }
//     return 0;
// }