// #include <stdio.h>
// #include <string.h>

// int main()
// {
//     /*
//         需求：字符串
//         题目描述：
//         给定字符串 s,t。

//         现在你要在 s,t 中删除一些字符并将它们重新排列使 s=t。

//         问操作后的 ∣s∣（即字符串 s 的长度）最大是多少？

//         输入格式：
//         第一行一个字符串 s。

//         第二行一个字符串 t。

//         输出格式：
//         一行一个整数，表示操作后的 ∣s∣ 的最大值。

//         说明/提示：
//         在第一个样例中，将 a 删除，留下 bc。

//         此时 ∣s∣=2，可以证明这是最优解。

//         在第二个样例中，将 aaaaa 删除，留下空串。 将 bbbbb 删除，留下空串。

//         此时 ∣s∣=0，可以证明这是最优解。

//         本题采用捆绑测试。

//         记 n=max(∣s∣,∣t∣)。

//         Subtask	n≤	特殊性质	总分值
//         1	    10	    无	    25
//         2	    10^5    A	    25
//         3	    10^5    B       25
//         4	    10^5    无	    25

//         对于 100% 的数据，1≤∣s∣,∣t∣≤10^5，字符串均由小写字母组成。

//         特殊性质 A：s 是一个 a∼z 的排列。

//         特殊性质 B：保证 si,ti∈{a,b}。

//     */

//     /*
//         核心思路：
//         删除字符 + 重新排列后 s=t” 的本质：
//         求两个字符串的字符交集—— 即每个字符在 s 和 t 中出现次数的最小值之和
//         （重新排列不影响字符组成，只需保证共同字符的数量足够）。
//     */ 
//     // 定义两个计数数组，统计26个小写字母的出现次数（初始为0）
//     int count_s[26] = {0};
//     int count_t[26] = {0};

//     char s[100001], t[100001];
//     scanf("%s%s", s, t);  // 一次性读取两个字符串，高效

//     // 统计s中每个字符的出现次数
//     int len_s = strlen(s);
//     for (int i = 0; i < len_s; i++) 
//     {
//         count_s[s[i] - 'a']++;  // 'a'-'a'=0, 'b'-'a'=1, ..., 'z'-'a'=25
//     }

//     // 统计t中每个字符的出现次数
//     int len_t = strlen(t);
//     for (int i = 0; i < len_t; i++) 
//     {
//         count_t[t[i] - 'a']++;
//     }

//     // 计算每个字符的最小出现次数之和（即最大共同长度）
//     int max_len = 0;
//     for (int i = 0; i < 26; i++) 
//     {
//         // 取s和t中该字符出现次数的最小值，累加到结果
//         max_len += (count_s[i] < count_t[i]) ? count_s[i] : count_t[i];
//     }

//     printf("%d\n", max_len);
//     return 0;
// }