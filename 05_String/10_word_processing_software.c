// #include <stdio.h>
// #include <string.h>

// int main()
// {
//     /*
//         需求：文字处理软件
//         题目描述：
//         你需要开发一款文字处理软件。最开始时输入一个字符串作为初始文档。可以认为文档开头是第 0 个字符。需要支持以下操作：

//         1 str：后接插入，在文档后面插入字符串 str，并输出文档的字符串；
//         2 a b：截取文档部分，只保留文档中从第 a 个字符起 b 个字符，并输出文档的字符串；
//         3 a str：插入片段，在文档中第 a 个字符前面插入字符串 str，并输出文档的字符串；
//         4 str：查找子串，查找字符串 str 在文档中最先的位置并输出；如果找不到输出 −1。
//         为了简化问题，规定初始的文档和每次操作中的 str 都不含有空格或换行。最多会有 q 次操作。

//         输入格式：
//         第一行输入一个正整数 q，表示操作次数。

//         第二行输入一个字符串 str，表示最开始的字符串。

//         第三行开始，往下 q 行，每行表示一个操作，操作如题目描述所示。

//         输出格式：
//         一共输出 q 行。

//         对于每个操作 1,2,3，根据操作的要求输出一个字符串。

//         对于操作 4，根据操作的要求输出一个整数。
    
//         说明/提示：
//         数据保证，1≤q≤100，开始的字符串长度 ≤100。
//     */

//     // 1. 调整数组容量（足够容纳最大长度：初始100 + 100次操作×每次插入100字符 = 10100）
//     char str[10100];
//     int q;

//     // 读取操作次数和初始字符串
//     scanf("%d", &q);
//     scanf("%s", str); // 数组名本身是地址，无需&

//     // 2. 循环处理q次操作（核心：每次操作基于当前文档str）
//     for (int i = 0; i < q; i++) 
//     {
//         int op;
//         scanf("%d", &op); // 读取当前操作类型

//         switch (op) 
//         {
//             // 操作1：后接插入 str_add → 输出更新后的文档
//             case 1: 
//             {
//                 char str_add[101];
//                 scanf("%s", str_add);
//                 strcat(str, str_add); // 直接在原文档末尾拼接
//                 printf("%s\n", str);
//                 break; // 必须加break，避免穿透
//             }

//             // 操作2：截取 a开始的b个字符 → 输出更新后的文档
//             case 2: 
//             {
//                 int a, b;
//                 scanf("%d %d", &a, &b);
//                 char temp[10100]; // 临时存储截取结果
//                 // 从str[a]开始，复制b个字符到temp
//                 for (int j = 0; j < b; j++) 
//                 {
//                     temp[j] = str[a + j];
//                 }
//                 temp[b] = '\0'; // 手动加终止符，确保是合法字符串
//                 strcpy(str, temp); // 更新原文档为截取后的字符串
//                 printf("%s\n", str);
//                 break;
//             }

//             // 操作3：在第a个字符前插入str → 输出更新后的文档
//             case 3: 
//             {
//                 int a;
//                 char insert[101];
//                 scanf("%d %s", &a, insert); // 一次性读取位置a和插入字符串

//                 int len_str = strlen(str);
//                 int len_ins = strlen(insert);

//                 // 核心：移动a位置后的字符（从后往前移，避免覆盖）
//                 memmove(str + a + len_ins,  // 移动后的起始位置
//                         str + a,            // 移动前的起始位置
//                         len_str - a + 1);   // 移动长度（包含原'\0'）

//                 // 复制插入字符串到a位置
//                 strncpy(str + a, insert, len_ins);

//                 printf("%s\n", str);
//                 break;
//             }

//             // 操作4：查找子串str → 输出最先位置（未找到输出-1）
//             case 4: 
//             {
//                 char target[101];
//                 scanf("%s", target);
//                 char *pos = strstr(str, target); // 库函数：查找子串第一次出现的指针
//                 if (pos != NULL) 
//                 {
//                     // 指针差值 = 索引位置
//                     printf("%ld\n", pos - str);
//                 } 
//                 else 
//                 {
//                     printf("-1\n");
//                 }
//                 break;
//             }
//         }
//     }

//     return 0;
// }